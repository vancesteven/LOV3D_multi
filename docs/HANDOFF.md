# pylov3d — Machine Coordination Channel

> Single shared coordination file. Every session on any machine: **pull, read
> this file, work, update this file, commit `coord:`, push.** Keep this file
> under ~150 lines — task detail lives in `docs/tasks/`, not here.

---

## Sync protocol (git is the channel, not Dropbox)

Machines have **separate clones**; the fork on GitHub is the single source of
truth. Refer to the push target by URL — remote *names* differ per machine:

- **Push target (always):** `https://github.com/vancesteven/LOV3D_multi`
  (Machine A: remotes `origin` *and* `myfork`; Machine B: remote `myfork`.)
- **Never push to** `https://github.com/mroviranavarro/LOV3D_multi` (upstream).

**Locking = git non-fast-forward rejection.** To claim a task:
1. `git pull` → confirm task is `free` in the ledger below.
2. Edit ledger (set Owner), commit `coord: claim TASK-NNN`, **push immediately**.
3. Push rejected → someone else moved first: pull, re-read ledger, retry or stand down.

Only then start work. Same dance to release (`coord: release TASK-NNN`, with a
log line). Coordination commits (`coord:` prefix, touching only `docs/HANDOFF.md`
and `docs/tasks/`) are pre-authorized — no per-commit user approval needed.
**Code commits still require user approval** (standing rule unchanged).

---

## Task ledger

One row per task. Owner ∈ {A, B, CODEX, free}. Never work a task you don't own.

| ID | Task | Owner | Status | Spec |
|---|---|---|---|---|
| TASK-001…004 | Milestone 4 (coupled JAX port, Aprop_aux, MATLAB validation, benchmark) | — | ALL VERIFIED & committed (see log / git history) | `docs/tasks/` |
| TASK-005 | **M5:** audit + fix + test the 1D ocean path (3 bugs incl. singular BC matrix) | free | VERIFIED — committed `9ce8e78` | tests: `test_solver_ocean.py` |
| TASK-006 | **M5:** `assemble_bc_ocean_coupled` (24N×24N) | free | VERIFIED — committed `3953c29` | `docs/tasks/TASK-006-codex-bc-ocean-coupled.md` |
| TASK-007 | **M5:** coupled NumPy solver ocean path | free | VERIFIED — committed `a68a351`. All 5 MATLAB/Qin checks live+green | log entry below |
| TASK-008 | **M5:** MATLAB/Qin reference harness (Weber Moon) | free | VERIFIED — committed `61f1292`; all 5 checks live+green since 007 | `test_matlab_validation_ocean.py` |
| TASK-009 | **M5:** JAX coupled ocean support (three-segment scan) | free | VERIFIED — committed `6374640` | log entry below |
| TASK-010 | ocean energy dissipation (ocean-ceiling node in `energy.py`) | free | **ON HOLD — B STAND DOWN** (user direction 2026-08-03, supersedes "work the candidate list"). Recon preserved in spec; resume later | `docs/tasks/TASK-010-ocean-energy.md` |
| TASK-011 | **M6:** Mars 1D reference model | free | VERIFIED — committed `4836150` (2 science-review rounds) | `docs/MARS_MODEL.md`, `pylov3d/mars.py` |
| TASK-012 | **M6:** body-agnostic forward-model + MC framework + mapping + Mars fit map | free | VERIFIED — committed `ecef078` (with 013) | log below |
| TASK-015 | **M6:** full pocoMC Mars posterior production run (n_active≥256, Nrbase=100, tens of minutes; publish corner plot + medians vs point fit). **Machine B** — compute-heavy, token-light | CODEX | **DONE — uncommitted pending commit gate**: production pocomc run used n_active=256, n_effective=512, dynamic SMC, Nrbase=100, seed 0; 4288 samples, ESS=4092.5, wall=5371.7 s. Published chain + PDF/PNG corner plot; all deterministic point-fit parameters lie inside posterior 68.26% intervals. Fast suite 633 passed, 30 deselected. **Committed `e7645a3`; VERIFIED (A, 2026-08-12)** — chain re-analyzed on A: n_active=256/Nrbase=100 as the ticket specified, 4288 samples, ESS 4092.5 reproduced, medians rho_core 6113.7 (+89.1/−93.2), rho_lm 4135.5, mu_scale 0.9770, R_core 1835.9 (+35.1/−35.2); point fit inside every 68.26% interval. Fig2 + chain now come from the converged run, not the smoke config. | `docs/MARS_MODEL.md`, `docs/figures/proposal/mars_posterior_chain.npz`, `docs/figures/proposal/fig2_mars_posterior.{pdf,png}` |
| TASK-016 | **M6:** lateral-field stage (Airy + areoid correction, n_lv≤4) | free | VERIFIED — committed `462d78a` | `docs/tasks/TASK-016-design.md` |
| TASK-014 pt 2 | **M6:** B — MATLAB coupled cross-check of the Mars lateral model | B | **DONE — committed `8a134e3`**: native LOV3D coupled solver on the 4-layer Mars model + committed crust `mu_variable` field reproduces the Python lateral spectrum — N=115 coupled modes (exact), k2_uniform identical to 12 digits, forcing-mode k2 rel err **2.95e-13**, k2 lateral shift 5.5174e-5 confirmed, non-forcing spectrum matches mode-for-mode. Awaiting VERIFIED (different-driver review). | `scripts/mars_lateral_cross_check.m` |
| TASK-017 | test-suite runtime lanes | free | VERIFIED — committed `c14a268` (fast 483/~2min, full 496 via `-m ""`) | `docs/tasks/TASK-017-codex-suite-runtime.md` |
| TASK-018 | **M7:** Moon instantiation (Weber reference, 4-param MC, citation-verified constraints, core_layer_index framework fix) | free | VERIFIED — committed `c14a268` (3 Opus rounds) | `docs/MOON_MODEL.md` |
| TASK-020 | **CRITICAL (user): commit verification artifacts for both Mars MATLAB cross-checks** — the 1D (k2/h2/l2 ~1e-12) and coupled-lateral (N=115, 2.95e-13) numbers currently exist only as prose; a MATLAB-less reader cannot verify them. B: re-run both drivers, save console output + a small .mat of the computed Love numbers/spectrum to `data/tests/mars/` (mirroring `data/tests/moon/` practice), commit. Also state MATLAB version used | B | **DONE — committed `08a38b6`** (MATLAB R2025b, 25.2.0.3150157 Update 4). Both drivers re-run; `.log`+`.mat` for each in `data/tests/mars/`; 1D k2/h2/l2 ~1e-12, coupled N=115 & k2 rel 2.95e-13 reproduced; artifacts scipy-readable (MATLAB-less verify OK); MARS_MODEL.md cites them. **VERIFIED (A, 2026-08-07)** — both .mat scipy-verified on A: 1D k2/h2/l2 match Python pins to ~1e-13; lateral N=115 + full spectrum present. | `data/tests/mars/` |
| TASK-019 | **M7:** properly-resolved Moon posterior under the shipped bounds (n_active>=64, n_effective>=128; the quoted 363.7±33.2 km reference run predates the density floor). **Machine B** — compute-heavy, token-light | B | **DONE — committed `e165ee0`.** pocoMC n_active=64/n_effective=128/Nrbase=50, dynamic term; n_samples=4105, ESS=4087.7, wall 123 min. R_fluid_core=**321 km (−13.8/+19.1)** (core_rho_scale 0.900 at floor, mu_scale 0.965, mantle_rho_scale 1.01); observables match all 4 constraints. 321 km < stale pre-floor 363.7±33.2 km, pulled toward as-built Weber ~327 km — mass/MoI mechanism under the 0.88 floor, as moon_mc.py predicts. Python suite still green (488 passed). **VERIFIED (A, 2026-08-07)** — chain re-analyzed on A: ESS 4087.70 and all medians reproduce. MOON_MODEL.md reference-posterior section updated (`2470969`). | `docs/figures/proposal/moon_posterior_chain.npz` |
| TASK-013 | **M6:** Mars SH data loaders (GMM-3 SHADR + MarsTopo719) | free | VERIFIED — committed `ecef078` (with 012) | `docs/tasks/TASK-013-codex-mars-sh-loaders.md` |
| TASK-014 | **M6:** MATLAB Mars cross-checks (both parts) | free | **VERIFIED (A, 2026-08-05)** — pt 1 `a07bbf8` (1D, ~1e-12); pt 2 `8a134e3` (coupled lateral: N=115 exact, k2 rel 2.95e-13, spectrum mode-for-mode; A checked the Python-side anchors, the npz contract in the driver, and that pylov3d/ is untouched — MATLAB execution B-attested per part-1 precedent) | `scripts/mars_1d_cross_check.m`, `scripts/mars_lateral_cross_check.m` |
| TASK-021 | **Hydration-front tidal signature (proposal RQ1 made quantitative):** serpentinization front in the crust shell (f_h, serpentinite mu/K reductions, lateral distribution tied to ingested crustal-thickness fields) -> mu_variable + K_amp through the validated lateral machinery -> predicted k2 shift + Love spectrum vs f_h, detectability statement vs sigma_k2=0.006 | A | **DONE — committed `370234d`** (2 Opus review rounds + verified fix round). Headline: Δk2 max ~5% of σ_k2 at f_h=0.5, robust across 12× reference-crust span; mean:lateral ~65:1 at lmax=4; k2 = bulk-hydration meter, front location lives in off-(2,0) spectrum. 20 tests; fast lane 502. **VERIFIED (2026-08-08)** — B's independent Nrbase=50 sweep (TASK-021b, `1a0d26a`) reproduces every headline number to <0.15% | `pylov3d/mars_hydration.py`, `docs/MARS_MODEL.md` §5, fig7 |
| TASK-021b | **B: hydration-amplitude sweeps** — run `get_love_hydrated` over the f_h × serpentinite-bracket grid at higher radial resolution (Nrbase≥50) + lmax=4, confirm A's Δk2 curve + ~65:1 mean:lateral ratio on independent hardware, optionally extend to lmax=6 to check (4,0)↔(2,0) channel convergence. Compute-heavy, token-light. Entry: `pylov3d/mars_hydration.py` docstring + `docs/MARS_MODEL.md` §5 | B | **DONE — committed `1a0d26a`** (`scripts/mars_hydration_sweep.py`; artifacts under `docs/figures/proposal/mars_hydration_sweep.{npz,png}`). Independent sweep at **Nrbase=50** (finer than A's 30), lmax=4, wall 3621 s: reproduces A's central-ratio mean/lateral/ratio to **<0.15% on every f_h** (e.g. f_h=0.5: mean 3.2032e-4, lateral 5.622e-6, ratio 57.0 vs A's 3.203e-4/5.62e-6/57). **mean:lateral 57–64:1 confirmed** on independent hardware+grid (A's ~60:1, not 65:1, corroborated). Detectability reproduced (Δk2 max 5.43% of σ_k2 at f_h=0.5). **lmax=6 convergence check** (Nrbase=30, central): lateral term shifts only 0.33%/1.09%/2.05% at f_h=0.1/0.3/0.5 (N 115→219) — (4,0)↔(2,0) channel converged at lmax=4, A's config vindicated. No pylov3d module modified. **VERIFIED (A, 2026-08-08)** — npz re-analyzed on A: central mean terms match A's pins to 4 digits (6.3357e-5/1.9106e-4/3.2032e-4 at f_h=0.1/0.3/0.5), ratios 64.1/60.9/57.0, total 5.43% of sigma at f_h=0.5, precision-to-resolve 6.43e-5; driver imports only mars_hydration, diff touches no pylov3d module. TASK-021 chain closed. | — |
| TASK-022 | **Codex: per-file Apache-2.0/SPDX headers** across pylov3d + Python scripts, README test-count refresh. Mechanical, no network | CODEX | **DONE — committed `c4bb06e`** (86 files, header-only diff verified: zero changed lines outside the inserted block). README counts corrected to 546/521/39 in `f96cef9`. Awaiting VERIFIED |  `docs/tasks/TASK-022-codex-license-headers.md` |
| TASK-024 | **M8 prep: fetch Moon SH field data** | A | **DONE — committed `0ac6a0f`** — `data/moon/`: `grgm900c_120_sha.tab` (GRGM900C truncated 900→120; original sha256 matches pyshtools' own pinned hash — independent integrity confirmation) + `MoonTopo719.shape.gz` (MoonTopo2600p truncated to 719 + whitespace→comma reformat, values verbatim). Both load via the existing Mars loaders; r0 1738.0 km, GM 4902.7999671, J2 2.0327e-4 (pub. 2.0331e-4), shape C00 → 1737.1512 km matching `moon.MOON["R"]` to 6 s.f. via no shared code path. SOURCES.md pins sha256 of stored AND original files + exact reproduce steps | `data/moon/SOURCES.md` |
| TASK-023 | **Codex: Moon SH loader hardening + tests** — make `load_shape` delimiter-agnostic (removes the need to ever manufacture a reformatted shape file again), Moon gravity/shape loader tests, one Moon geographic validation test (SPA basin or nearside/farside dichotomy) mirroring the Mars Hellas test | CODEX | **DONE — committed `bb71493`. VERIFIED (A, 2026-08-11).** All three spec parts landed, including the *strong* geographic check rather than the weaker nearside/farside fallback: MoonTopo719's global minimum at lmax=90 lands 75S/135W, **781 km from the published SPA centre** (53S/169W) and well inside the basin's ~1200 km semimajor axis. A checked, not assumed: the assert box covers only **4.2% of the lunar surface**, so it is a real constraint, not a trivially-passing test; the docstring correctly handles both Moon-specific traps the spec flagged (principal-axis longitude convention, and degree-1 retained deliberately with reasoning); the Part-A malformed-input guard is present and raises. **Citation caveat worth institutionalizing:** Codex has no network, so its Garrick-Bethell & Zuber (2009, Icarus 204, 399-408) citation was written *from memory*. A verified it on A — title, volume, pages and the 53S/169W centre all check out exactly — but that is luck, not process. **Every Codex-authored citation must be verified by A before it ships**, and future Codex specs should say so explicitly. | `docs/tasks/TASK-023-codex-moon-sh-loaders.md` |
| TASK-025a | **Anelasticity stage a:** validate the viscoelastic 1D solver path against PyALMA3 (Maxwell + Andrade if supported), anelastic complex-k2 forwards for as-built Mars (verified forcing period) + Weber Moon (monthly tide); headline: does closing the Moon's 4.6% elastic k2 gap imply Q consistent with 38±4? Literature prior ranges for stage b | A | **DONE — committed `f96cef9`** — build + Opus REJECT + 13-fix round + independent citation audit (10 further fixes), fast lane 521 green, A verified no solver module touched (all non-025a diffs are exactly the 7-line SPDX header) and the sign-mutation now fails the headline tests. Citation re-verification CLEAN (10 minor fixes applied); **gate-ready** — port implements Maxwell ONLY (no Andrade anywhere, incl. vendored MATLAB; guard test added). Maxwell↔PyALMA3 agrees ~3-6e-5 rel. Claimed headline (UNDER REVIEW): closing the Moon's 4.6% elastic k2 gap needs Maxwell η=1.8e16 → Q≈0.79 (9.3σ from Williams & Boggs 38±4, i.e. Maxwell inadequate), vs Andrade α=0.35 → Q≈36.9 (0.3σ, consistent). Caveats flagged by builder: Andrade numbers are EXTERNAL PyALMA3 (not pylov3d), PyALMA3 is incompressible + center-fluid-only so the Moon Andrade run uses a simplified body. Mars k2=0.169 confirmed solar-semidiurnal, not comparable to Bills Phobos-tide Q. 29 tests, fast lane 521 | `pylov3d/anelastic.py`, `anelastic_moon.py` |

| TASK-025b | **B: joint (rigidity, viscosity, alpha) Moon fit** — does adding Q=38±4 break the rigidity/anelasticity degeneracy? | B | **DONE — committed `6ef44d3`.** Full 2×2 {Maxwell,Andrade}×{±Q}, all ESS>4000. **VERIFIED (A, 2026-08-10)** — all four chains re-analyzed independently on A: every median, interval and correlation B reports reproduces exactly, and the strongest internal check passes — **Maxwell−Q recovers mu_scale=0.9650, the TASK-019 elastic answer**, i.e. the new 6-param machinery reproduces the old 4-param result when anelasticity is switched off. Likelihood read and confirmed sound (4 structural/k2 Gaussians + optional Q via the reviewed `implied_Q`, uniform box priors). **One correction to the headline (A):** "Q does not break the degeneracy" is too absolute. B is right that Q does not *relocate* mu_scale (1.007→1.012, within 1σ) and does not destroy the mu–alpha ridge (−0.673→−0.697) — but Q **does** add real information about rigidity: mu_scale's 68% width tightens **2.69×** (0.0944→0.0351) and log10_eta **1.87×**, and corr(mu_scale, log10_eta) **flips sign** (−0.666→+0.510), unremarked by B. Mechanism: without Q, eta roams 2 decades and trades against mu; Q pins eta to ±0.5 dex, which drags mu with it. Accurate statement: **Q substantially reduces the degeneracy without eliminating it** — the mu–alpha ridge survives, so Q still cannot cleanly partition the k2 gap between rigidity and rheology. alpha=0.286 (+0.066/−0.052) confirmed, only 1.09× tighter than its prior-driven noQ width, so alpha is weakly informed. | `docs/tasks/TASK-025b-joint-anelastic-fit.md` |
| TASK-026 | **Off-(2,0) detectability — the proposal's unclosed loop.** Methods_Models defers it in writing twice ("detectability ... explicitly future work"), yet RQ1's "detectable signatures in gravity" rests on it. Convert the validated spectrum (largest off-mode (3,0) at \|k\|=7.29e-5) into a required gravity-field precision and compare against achieved (Mars seasonal ΔC20/ΔC30 recovery) and plausible-future (GRAIL-class) precision. A negative, quantified answer is a fine result — it defines a measurement requirement, as TASK-021's null did | A | **DONE — committed `cb67b1e`.** Two distinct observables, both undetectable today: **diagonal k2m splitting** (|dk20|=5.52e-5, |dk21|=2.09e-5, |dk22|=3.40e-5) needs **8.2x/12.0x/8.2x** better than GRAIL's achieved lunar precision (Konopliv 2013 Tab.4); **off-diagonal spectrum** top mode (3,0) needs ~28x better than demonstrated seasonal degree-3 recovery. **REVIEW: REJECT → all 12 findings fixed and re-derived, committed `6cd7a51`.** Corrected: √2 basis norm on m≠0 modes (tail 550x→388x, (3,0) unaffected); `ratio_grail` dropped as a category error; GSFC GRAIL analysis added (41/16/12x alongside JPL 8.2/12.0/8.2x, the two disagreeing on k22 by 27x the predicted splitting); Moon-vs-Mars quantified (ξ2 ratio 736 → instrumental requirement ~6-8.8e3, not ~10x); unverified k3 attribution deleted; 28x/66x bracket, lmax non-convergence and the m=0 frequency degeneracy all disclosed. 591 tests. Proposal corrected first (`e12425c`). Awaiting VERIFIED | `docs/tasks/TASK-026-off-forcing-detectability.md` |
| TASK-027 | **B: lateral robustness** — (1) truncation convergence past the single lmax=5 spot check (8.3% change is one step, not convergence), tracking the forcing mode AND the detectability-relevant off-modes, with the Nrbase-independence assumption verified rather than assumed; (2) Airy crust-parameter sensitivity sweep, the analogue of TASK-021's reference-crust table; **(3) NEW, added 2026-08-11 — MATLAB cross-check of the m=1,2 diagonal forcing solves.** TASK-026's proposal-cited k2m splitting rests on `MARS_K21_FORCING`/`MARS_K22_FORCING` (0.16902091346458947 / 0.16903399541441133 → |dk21|=2.09e-5, |dk22|=3.40e-5). Only the m=0 spectrum has a native-MATLAB anchor (`8a134e3`, 2.95e-13); the m=1,2 solves do not, and the proposal now quotes 8.2x/12.0x/8.2x ratios derived from them. Extend `scripts/mars_lateral_cross_check.m` to run forcing (2,1) and (2,2) on the same committed model+`mu_variable` field and confirm both. Compute-heavy, token-light | B | **DONE — committed `b28460e`. VERIFIED (A, 2026-08-11)** — every number re-derived on A from the committed artifacts: MATLAB m=0/1/2 match the Python constants to **7.0e-12** (the proposal-cited diagonals now have their native anchor); convergence sequence, Nrbase-independence (4.5e-8 spread) and the ×7.6 Airy spread all reproduce exactly; no pylov3d module touched. **A adds one consequence B did not draw:** the truncation error is *mode-dependent and large*, so it propagates unevenly into TASK-026's proposal-cited detectability table — (3,0) 28.5→**24.4** (signal 17% stronger when converged, requirement *easier*), (2,±2) 38.6→38.1 (stable), (3,±1) 62.4→**95.0** (mode overstated 52% at lmax=4), diagonal Δk20 ratio 8.2→**7.2** JPL / 41.3→36.6 GSFC. A blanket truncation caveat is therefore the wrong instrument — the corrections differ by mode and in both directions | `docs/tasks/TASK-027-lateral-robustness.md` |
| TASK-028 | **Non-Airy crustal-model substitution — the dominant error term.** TASK-027 measured the Airy crust-parameter spread at ×7.6 against the truncation ladder's ×1.13, so the Airy assumption, not the harmonic cutoff, is where the lateral stage's remaining error lives. Data landed (`c2a8dbe`): five InSight-calibrated Moho SH models (Wieczorek et al. 2022, Zenodo 6477509, CC-BY-4.0), all at crust density 2900 and mean thickness within 1 km of the project's 50 km reference, so the swap changes the lateral *pattern* only. Build: loader → dt contract, diagnostics, 5-model + Airy comparison at validated lmax=4, docs section | A | **DONE — committed `196670c`** (data `c2a8dbe`). Adversarial review REJECT → all 9 findings fixed and re-verified. Headline: **(2,0) is a second first-order channel and cancels 91.4% of the (4,0) result** in a C20-retaining field. Airy pattern corroborated (corr 0.95–0.97). Proposal corrected (`1926058`). 624 tests. Awaiting VERIFIED | `data/mars/insight_moho/SOURCES.md` |
| TASK-029 | **B (TOP PRIORITY): native-MATLAB anchor for the two first-order zonal channels.** TASK-028's new result — (2,0) and (4,0) both couple the degree-2 zonal tide to itself at first order, with **opposite signs cancelling ~91%** — is cited in the proposal and is the **only** link in the Mars chain with no independent anchor. Reproduce the scaling exponents ((2,0)→1.003 new; (4,0)→1.001 and (3,0)→2.003 as controls) and the signed contributions (−1.5901e-5 / +1.4528e-5 / −1.3738e-6). **Signs matter more than magnitudes.** Extend `scripts/mars_lateral_cross_check.m`; artifacts to `data/tests/mars/` | B | **DONE `78e7c92` — VERIFIED (A, 2026-08-11).** A re-analyzed the `.mat`: exponents (2,0) **0.9988**, (4,0) 1.0001, (3,0) 2.0005 (both controls reproduce the documented 1.000/2.002); signed contributions −1.58583e-5 / +1.45308e-5 / −1.33977e-6, **signs identical to Python**, magnitudes to 0.27%/0.02%/2.48%, cancellation 91.6% vs 91.4%; coupling array 0, 0.6389, 0, 0.8571, 0, 0 for n_b=1..6, exact (0.857142857142857 = 6/7). **Proposal text stands — no pullback.** | `docs/tasks/TASK-029-matlab-first-order-channels.md` |
| TASK-030 | **B: close the m-ordering caveat.** The proposal states that the ordering among m (which of k2m splits most) "should not be relied upon", because m=1 and m=2 were never run above lmax=4 while m=0 was carried to lmax=6. Run the m=1,2 diagonal forcing solves at lmax=5 and 6 (same config as TASK-027 part 1) and report whether the ordering is stable. Compute-heavy, token-light; directly retires a live proposal caveat | B | **DONE `cbae776` — VERIFIED (A, 2026-08-11).** npz re-analyzed: ordering **m=0 > m=2 > m=1 at lmax=4, 5 and 6 alike**; closest pair (m=2 vs m=1) separated by 46–63% of the smaller shift, never approaching a crossing; lmax=4 row reproduces the proposal constants to **1.3e-15** (B claimed ≤0.01% — it is essentially exact). **Proposal caveat retired (`9ff7419`).** |dk2m\| ordering **m=0 > m=2 > m=1 holds at every lmax**; lmax=4 row reproduces MARS_K21/22_FORCING to ≤0.01%; each shift grows monotonically, closest pair (m=2 vs m=1) never approaches a crossing. Artifacts `docs/figures/proposal/mars_m_ordering.{npz,png}` + `data/tests/mars/mars_m_ordering.log`; note in MARS_MODEL.md §6. Code commit pending PI approval. | — |
| TASK-031 | **B: Moon lateral stage (M8 kickoff).** Data and loaders both landed (`0ac6a0f`, `bb71493`). Build the Moon analogue of TASK-016: GRAIL/LOLA → Airy crustal thickness → crust rigidity → coupled Love spectrum, reusing `mars_lateral.py`'s structure. Note the Moon's degree-1 centre-of-figure offset (~1.9 km) is far larger than Mars's and needs a deliberate decision, and that TASK-028 showed the degree-2 convention is a first-order lever — decide both explicitly rather than by mirroring the Mars code | B | **PIPELINE STAGE DONE — committed `67dd097`** (A landed the previously-untracked implementation so it is unambiguously in B's clone, not only in a shared worktree). Module + 9 tests + driver + artifacts + MOON_MODEL.md section; both conventions decided explicitly and **pinned by tests**, incl. `test_c20_retention_crosses_rigidity_positivity_bound`. Δk20=+1.407e-6 = 0.64% of σ_k2 → diagonal negligible, information in the off-forcing spectrum, mirroring Mars. **Angular convergence NOT established (lmax 2 vs 4 differ ×5.93) → split out as TASK-031b.** | `docs/tasks/TASK-031b-moon-lateral-convergence.md` |
| TASK-032 | **Codex: refresh the test-count claims and add a guard so they stop drifting.** README says 546/39-files/521-fast; actual is 653/43/624 — ~150 adrift, the *fourth* such drift. Part A corrects it; **Part B is the real deliverable**: a non-circular test that fails when the README claim stops matching reality. Spec flags the two traps — a self-comparing test proves nothing, and reading counts from `request.session.items` breaks under partial invocation (running that file alone would see one item) — and says to ship the file-count check alone if the full guard cannot be made robust | CODEX | **DONE `c2690c4` — VERIFIED (A).** README → 654/44/625; guard demonstrated: A broke the count deliberately, `test_readme_validation_counts_match_live_collection` failed, passed on restore, and it survives partial invocation (the trap the spec named). | `docs/tasks/TASK-032-codex-test-count-guard.md` |
| TASK-033 | **Codex: make the README guard robust to in-flight work.** TASK-032's guard is good and A verified it catches drift — but its *file* count is exact while test counts are tolerant, so another machine's uncommitted `test_moon_lateral.py` reds the suite for everyone. That is the "brittle in a way that annoys" outcome the 032 spec warned against. Options given (tolerance, git-tracked-only, or warn-not-fail); **acceptance is a green suite WITH the in-flight files present** | CODEX | **DONE — committed `1e71c06`. VERIFIED (A, 2026-08-12) with one completion by A.** Codex chose git-tracked-only for the file count and wrote the trade-off into the docstring as asked. A tested the real acceptance condition (untracked module present) and found a residual asymmetry: the file count became tracked-only while test counts still came from live collection, so an in-flight module larger than the ±10 tolerance would still red-light the suite — not hypothetical, this repo shipped `test_anelastic.py` with 29 tests. A closed it (`--ignore` on untracked modules) so both halves measure the repository. Verified: green with a simulated 40-test untracked module present, still fails on a 200-test README drift. Suite 633. | `docs/tasks/TASK-033-codex-guard-refinement.md` |
| TASK-031b | **B: Moon lateral truncation convergence.** Moon analogue of TASK-027 part 1 — lmax=4/5/6(/7) ladder on Δk20 and the top off-modes ((2,±2), (2,±1), (3,±3)), with Nrbase-independence **verified rather than imported from Mars** (the Weber Moon has a fluid outer core Mars lacks, so radial behaviour is not automatically the same). Cheap high-value extra: the Moon field has **no (2,0) channel at all** (C20 removed), so confirming (4,0) alone is first order tests whether TASK-028's ~91% cancellation is Mars-specific or general | B | **DONE — awaiting VERIFIED (uncommitted, pending commit gate).** Finding: the linearized Airy pipeline **cannot be carried past lmax=4** — `max\|dmu/mu_bar\|` crosses the positivity bound between lmax=4 (0.9902) and lmax=5 (1.1531), so lmax=5,6 are blocked by design. The physical ladder is lmax=2,3,4 and Δk20 is still climbing steeply there (+90.5%, +211.2%), so **TASK-031's Δk20=1.407e-6 is the highest-lmax value the linearization admits, NOT a truncation-converged endpoint** (no extrapolation offered — this is the finding per spec pt 4). Radial convergence **re-verified for the fluid-core Moon** (not imported): Nrbase 30 is converged to 1.25e-6 vs Nrbase=50. (4,0) channel exponent **1.0001 = first order**, so TASK-028's first-order (even,0)→(2,0) coupling **generalizes** beyond Mars. Peak RSS 10.8 GB at lmax=4/Nrbase=50. | `docs/tasks/TASK-031b-moon-lateral-convergence.md`, `docs/MOON_MODEL.md`, `scripts/moon_lateral_convergence.py`, `docs/figures/proposal/moon_lateral_convergence.{npz,png}` |
| TASK-034 | **B (TOP): predicted Moon k2m splitting vs GRAIL's actual measurement.** The cleanest test in the project — **same body, same coefficients, real published measurement, no cross-body extrapolation**, unlike TASK-026 which had to borrow lunar precision and correct by ×736. Diagonal (2,0)/(2,1)/(2,2) at lmax=4 (031b closed the ladder above that) against GRAIL k20/k21/k22 ± 4.5/2.5/2.8e-4. **Expect a null**: observed GRAIL spread ~2.0e-4 is *smaller than the individual uncertainties*, so it is likely not significant, and the prediction should sit well below both — that is the publishable outcome, do not tune toward detection | free | QUEUED — **do TASK-036b first** (a shell change would invalidate these numbers; see log) | `docs/tasks/TASK-034-moon-k2m-vs-grail.md` |
| TASK-035 | **B: native-MATLAB anchor for the Moon lateral spectrum.** Mars lateral has one (N=115, 2.95e-13, `8a134e3`); the Moon lateral stage (`67dd097`) has none. Same pattern as TASK-029: export the committed Moon `mu_variable` field to npz so MATLAB reads the *identical* field, run native LOV3D on the Weber model, confirm the 115-mode spectrum and Δk20=+1.407e-6. Artifacts to `data/tests/moon/` | free | QUEUED | — |
| TASK-036 | **Design + A: the lateral-amplitude wall is GEOMETRIC, not a solver limit.** Diagnosis: `δμ/μ̄ = dt·(μ_c−μ_m)/(T·μ_c)` is the *exact* Voigt volume-fraction average in a fixed shell — linear because volume fraction is linear, **not** because anything was expanded to first order, so raising `perturbation_order` cannot help. It exceeds unity when the excursion demands a negative material fraction. Moon: unity at \|dt\|=32.95 km, **less than the 40 km shell** — the binding factor is the ×1.2139 rigidity contrast, not shell fullness. Two testable fixes: **(A) thicken the shell** (T≥64.5 km gives lmax=6 a 0.8 margin; pure resolution change, falsifiable by a convergence-in-T test) or **(B) Reuss/harmonic mixing** (positive by construction, but changes physics at *every* amplitude and would move published numbers). Try A first | free | QUEUED | `docs/tasks/TASK-036-design-lateral-amplitude-wall.md` |
| TASK-036a | **Codex: pin the amplitude bound with tests + the Mars equivalent.** Parts 1 and 4 of TASK-036's scope; **no solver runs** — closed-form arithmetic on committed constants. Assert `_dmu_ddt_coeff` equals `(μ_c−μ_m)/(T·μ_c)` computed from the module's own constants (not the hardcoded value, which would restate the implementation), pin the 32.95 km unity crossing and the 0.9902/1.1531/1.2897 margins, then do the same arithmetic for Mars and report whether Mars is limited by contrast or by shell fullness. **If Mars is limited differently from the Moon, that is the interesting result** | CODEX | QUEUED — spec ready | `docs/tasks/TASK-036a-codex-pin-the-bound.md` |
| TASK-036b | **B: does the Love spectrum converge in shell thickness T?** Parts 2–3 of TASK-036. Sweep T (40/55/70/85 km) for the Moon at fixed crust/mantle contrast, lmax=4, and report whether the spectrum converges in T; **the 1-D background fit must be re-checked, not assumed**, since widening the shell smears the contrast radially. If it converges, rerun the 031b ladder at the chosen T and report whether angular convergence is finally reachable. **Change no shipped default** — diagnosis only; migration is a separate ticket because it moves published numbers | free | QUEUED — **run before TASK-034**, see log | `docs/tasks/TASK-036-design-lateral-amplitude-wall.md` |
Statuses: `QUEUED → IN-PROGRESS → DONE → VERIFIED` (verification by an Opus-tier
reviewer, done by a *different* driver than the implementer when practical).

---

## Model routing (economical defaults)

| Engine | Where | Use for | Cost posture |
|---|---|---|---|
| **Fable 5** | A, main loop | Orchestration, protocol, hard design calls, final math review | Most expensive — keep turns short, delegate early |
| **Opus 5** | A, subagent | Review/verification, delicate impl (e.g. A1/A2 coupled unroll) | Escalation tier |
| **Sonnet 5** | A, subagent | Default implementation + tests | Workhorse |
| **Haiku 4.5** | A, subagent | Recon, search, file inventory | Cheapest |
| **Codex 5.6** | A, separate tool | Self-contained, well-specified tasks run in parallel (test suites, mechanical ports) | Parallel track |
| **Opus 4.8** | B, main loop | Tasks B claims in the ledger; same delegation rules as before | — |

**Codex handoffs:** Codex shares no conversation context. Every handoff is a
spec file in `docs/tasks/` that stands alone: goal, exact files, constraints,
the verify command (`venvLOV3Dconv/bin/python -m pytest pylov3d/tests/ -q`),
and done-criteria. Ledger Owner = CODEX while it runs. Codex output is always
reviewed by an Opus-tier agent before Status moves DONE → VERIFIED.

**Codex guardrails (standing — every Codex spec MUST include these):**
1. Explicit allowlist of writable files; everything else read-only.
2. No state-changing git (commit/add/push/pull/branch/checkout/reset/stash).
3. No environment changes (installs, venv edits, shims, substitute envs) —
   if the prescribed runner is broken, stop and report, never improvise.
4. Never touch `matlab/`, `docs/HANDOFF.md`, or coord files (own spec's
   Completion notes excepted).
5. No deletions; no files beyond the allowlist; 500-line cap per file.
6. On ambiguity or apparent spec error: stop, record the question in
   Completion notes, finish only the unambiguous parts.
7. Suite must stay green; regressions are reported, not force-fixed.
Recommended launcher settings until trust is established: workspace-write
sandbox, network disabled, approval prompts on anything outside the sandbox.

---

## Environment notes (non-obvious)

- **Python venv:** `venvLOV3Dconv/bin/python` on BOTH machines (venvs are
  per-clone, not synced). Machine B: JAX 0.10.0. Machine A: rebuilt 2026-08-02 —
  JAX 0.10.2, py3nj compiled from source (needed `brew install gcc` +
  `CC=gcc-16 CXX=g++-16 FC=gfortran-16` for OpenMP), plus pyalma3 (module name
  is `alma`) and matplotlib.
  Tests: `venvLOV3Dconv/bin/python -m pytest pylov3d/tests/ -q`
- JAX x64 enabled at import; complex128 OK on CPU. First JIT call slow (~10–30s).
- MATLAB source = `src/` + `tests/*.mlx` (there is no `matlab/` dir — earlier
  rule wording was stale). Read-only port reference: targeted reads for porting
  are fine; never modify, never bulk-scan.

---

## Standing rules (both machines)

- Do NOT implement code unless explicitly instructed — summarize & propose first.
- Code commits only when the user asks. `coord:` commits pre-authorized (above).
- Keep files < 500 lines; tests in `pylov3d/tests/`; no working files in repo root.
- graphify re-runs only with user confirmation: `graphify run pylov3d/ --exclude pylov3d/tests/`

---

## Project status

- ✅ M1 1D solver · ✅ M2 3D lateral/mode coupling · ✅ M3 MATLAB cross-validation
- ✅ **M4 COMPLETE** (JAX coupled port + validation + benchmark)
- ✅ **M5 COMPLETE** (`b871153` README): oceans in 1D (TASK-005, incl. 3-bug repair), coupled NumPy (006+007, Weber Moon MATLAB/Qin spectra to ppm), coupled JAX (009, three-segment scan). Suite **377** confirmed on BOTH machines (A `6374640`; B on pull to `52621b3`).
- **ON HOLD (user decision 2026-08-03) — ALL ocean-world follow-ons**, resume later: (1) TASK-010 ocean energy dissipation (B's recon preserved in `docs/tasks/TASK-010-ocean-energy.md`); (2) Europa thick-shallow-ocean coupled MATLAB case (constrains gO — B/MATLAB); (3) chunked-vmap for large-N memory; (4) GPU-backend benchmark; (5) aux NaN-factory cleanup (compute_aprop_aux_coupled evaluates the elastic propagator at in-ocean nodes and discards it).
- **NEW DIRECTION: M6 = Mars model**, staged: TASK-011 first (1D radial model fit to published bulk constraints — mass/MoI/k2/InSight core — with full source citations and a science-accuracy review), then TASK-012 (lateral variations from gravity/topography fields; data-source decision deferred to its ticketing). B: stand down from TASK-010, await Mars tickets.

---

## Coordination log

_Newest on top. Format: `[machine][YYYY-MM-DD] note`. Cap at ~15 entries —
prune from the bottom when adding (git history keeps the rest)._

- `[A][2026-08-12]` **TASK-036 split and assigned: 036a → Codex (analytic), 036b → B (compute). Also reordering B's queue, for a dependency reason worth stating.** **The split follows the work's nature:** 036a is closed-form arithmetic on committed constants with no solver runs — Codex's profile exactly — while 036b is a T-sweep plus a possible re-ladder, which is B's. **The reorder: B should run 036b BEFORE TASK-034.** The Moon k2m-vs-GRAIL result is computed on the shipped 40 km shell; if 036b concludes the shell should widen, the Moon lateral spectrum moves and 034's predicted splittings move with it, so 034 done first would be invalidated by 036b. 034 is only ~3 solves, so redoing it is cheap — but doing it in the right order is cheaper still, and it avoids publishing a number that is about to change. **Guardrails carried into both specs:** neither may change a shipped default, because that moves already-published numbers; migration is explicitly a separate ticket contingent on 036b's answer. Codex's spec additionally forbids hardcoding `-3.0348e-05` in the pin test — computing the expected value from the module's own constants is what makes the test non-vacuous, and the spec says so with the reminder that a test restating its implementation has already cost this project twice (the √2 basis error, and the mu_variable conversion). Codex is also told it cannot verify citations offline, so it should prefer not citing and flag anything it does write.
- `[A][2026-08-12]` **Design conversation taken up: the lateral-amplitude wall is geometric, and A had been mis-describing it.** Both blocking results — Mars TASK-028 (`|δμ/μ̄|`=0.9689, density axis unreachable) and Moon TASK-031b (0.9902 at lmax=4, crosses unity at 5) — were logged as limits of "the linearized rigidity mapping". **That framing is wrong in a way that matters.** Reading `_dmu_ddt_coeff`: `δμ/μ̄ = dt·(μ_c−μ_m)/(T·μ_c)` is the **exact Voigt volume-fraction average** of crust and mantle inside a fixed shell of thickness T. It is linear in dt because volume fraction is linear in dt — *not* because anything was expanded to first order. So the approximation does not live in the solver, and **raising `perturbation_order` could never have helped**; had anyone tried that as the obvious next step it would have burned compute for nothing. What actually breaks is shell geometry: the expression exceeds unity when the excursion would demand a negative material fraction. Measured for the Moon (T=40 km, contrast 1.2139): unity at |dt| = 32.95 km, and lmax=4 already sits at 32.63. **Note the shell is not even full** — dt/T = 0.82; it is the ×1.2139 rigidity contrast that turns that into 0.99. Two candidate fixes are written up with their costs: **(A) thicken the reference shell** — bound scales as 1/T, so T≥64.5 km gives the Moon's lmax=6 a 0.8 margin; a pure resolution change, falsifiable by asking whether the spectrum converges in T, though the 1-D background fit must be re-checked rather than assumed; **(B) Reuss/harmonic mixing instead of Voigt** — positive for every fraction by construction, but it is a real physical modelling choice needing a loading-geometry argument, and it perturbs results at *every* amplitude, so it would move already-published numbers. Recommendation: A first, because it changes resolution rather than physics. **One change would unblock both bodies**, which is why this is worth doing before more lateral-amplitude work is queued.
- `[A][2026-08-12]` **Advice for B: TASK-034 then TASK-035.** **034 is the strongest comparison available anywhere in this project, and it exists only at the Moon.** The Mars detectability argument has one unavoidable soft step — to benchmark a Mars prediction, TASK-026 had to borrow GRAIL's *lunar* precision and correct across bodies by the ×736 tide-raising ratio, which is exactly where a reviewer pushes. At the Moon that step vanishes: **GRAIL measured k20/k21/k22 separately at the same body**, and TASK-031 built a Moon lateral model that predicts the splitting those same coefficients should show. Same body, same coefficients, real published measurement, zero extrapolation. The spec warns B of the expected shape up front so a null does not tempt anyone into massaging it: **the observed GRAIL spread (~2.0e-4) is smaller than the individual uncertainties (2.5–4.5e-4), so the measured order-to-order differences are probably not significant at all**, and the prediction should land well below both. "The prediction sits two orders of magnitude beneath both the precision and an insignificant observed scatter" is a clean, quotable, same-body result — stronger than a strained detection claim. It also bounds the ladder: 031b closed lmax above 4, so the spec tells B not to attempt one. **035 closes the last validation gap of the same kind TASK-029 closed for Mars** — the Mars lateral spectrum has a native-MATLAB anchor at 2.95e-13, the Moon lateral stage has none — and it should follow B's own improved TASK-029 method: export the committed field to npz so MATLAB consumes the *identical* field rather than re-deriving it. Not queued for B: the linearization wall (031b/028), which is a formulation question rather than compute, and the Moon detectability conversion, which needs an observable/noise analysis rather than solver time.
- `[A][2026-08-12]` **TASK-033 VERIFIED with one completion by A (`1e71c06`).** Codex picked git-tracked-only for the file count and — the habit worth reinforcing — wrote the trade-off into the docstring unprompted: a genuinely stale README can stay silent in a dirty tree until the files are indexed. Correct call, correctly disclosed. **But testing the actual acceptance condition rather than the happy path found a residual asymmetry:** Codex made the *file* count tracked-only while the *test* counts still came from live collection, which sees untracked modules. An in-flight module holding more than the ±10 tolerance would therefore still have failed the suite on its test count alone — precisely the failure the refinement existed to prevent. **Not a hypothetical at this project's scale:** the module that triggered the original problem had 9 tests, but this repository has shipped one with 29. A closed it by excluding untracked modules from collection too (`--ignore`), so both halves measure the repository rather than one machine's working tree, and recorded the reasoning in the docstring alongside Codex's. Verified both directions: green with a simulated 40-test untracked module present, still failing on a 200-test README drift. **Generalisable lesson: when a fix makes one half of a comparison aware of something, check the other half.** The guard now does what it was cut for — the README claim can no longer rot silently, and in-flight work on another machine can no longer red-light anyone's suite.
- `[A][2026-08-12]` **B's TASK-031b and TASK-015 both VERIFIED; TASK-015 committed (`e7645a3`). No Codex work landed — TASK-033 is still QUEUED and untouched.** **031b is a structural negative result and the most interesting thing in this batch:** the linearized Airy pipeline *cannot be carried past lmax=4 for the Moon*. A reproduced the positivity margins independently — 0.3471 / 0.6752 / **0.9902** / **1.1531** / 1.2897 for lmax=2..6, monotone, crossing unity between 4 and 5 — and confirmed the pipeline raises by design beyond that. So Δk20 is still climbing steeply (+90.5%, +211.2%) exactly where the physics runs out, and **TASK-031's 1.407e-6 is the highest value the linearization admits, not a converged endpoint**. B correctly offered no extrapolation. **The cross-body pattern is now the headline:** Mars hit the same wall from the other direction (TASK-028 left the density axis unreachable at |δμ/μ̄| = 0.969 against the same unit bound), so *the linearized rigidity mapping — not compute, not truncation — is the binding constraint on this entire class of investigation on both bodies*. Any further progress on lateral amplitude needs a non-linearized formulation, which is a design question rather than a ticket. **The cheap extra paid off:** the Moon's (4,0) exponent is **1.00008**, and since the Moon field has no (2,0) channel at all (C20 removed), this establishes that TASK-028's first-order (even,0)→(2,0) coupling **generalizes beyond Mars** rather than being a Mars peculiarity. B also re-verified Nrbase-independence for the fluid-core Moon instead of importing the Mars result, as the spec required. **TASK-015** finally delivers the production posterior the ledger has wanted since early on — n_active=256/Nrbase=100 exactly as specified, ESS 4092.5, and every deterministic point-fit parameter inside its 68.26% interval, so the production run corroborates the point fit rather than displacing it; fig2 and the archived chain now come from a converged run rather than a smoke config flagged non-converged.
- `[B][2026-08-12]` **TASK-031b DONE — awaiting VERIFIED (uncommitted, pending commit gate).** Ran the 4-part spec via `scripts/moon_lateral_convergence.py` (driver-only; no solver module touched). **The finding is that the linearized Airy pipeline cannot be carried past lmax=4.** The positivity margin `max|dmu/mu_bar|` is monotone in the truncation and crosses the unit bound between lmax=4 (0.9902) and lmax=5 (1.1531): at lmax=5,6 `mu_variable_from_topography` raises by design (non-positive linearized crust; at lmax=6 the Airy thickness also exceeds the 40 km reference). So the physical ladder is **lmax=2,3,4 only** and Δk20 is still climbing steeply where it runs out — 2.37e-7 (lmax2) → 4.52e-7 (+90.5%) → 1.407e-6 (+211.2%). **Therefore TASK-031's Δk20=1.407e-6 is the highest-lmax value the Airy linearization admits (lmax=4), NOT a truncation-converged endpoint** — angular convergence cannot be demonstrated by climbing lmax with this pipeline because the field goes non-physical first. No extrapolation offered (spec pt 4). The off-forcing pairs are far better behaved across the same rungs ((2,±2) −0.11% then −5.68%; (3,±3) flat +0.00% at lmax3→4), so the spatial spectrum carrying the lateral information is closer to settled than bulk Δk20. **Nrbase-independence re-verified for the fluid-core Moon (NOT imported from Mars):** at lmax=4, Δk20 = 1.407061e-6 / 1.407122e-6 / 1.407124e-6 for Nrbase 15/30/50 — production Nrbase=30 converged to 1.25e-6 vs 50; the angular truncation, not the radial grid, is binding. **(4,0) channel exponent = 1.0001 = first order:** with C20 dropped the Moon field has no (2,0) self-coupling, so (4,0) is the sole first-order driver of the (2,0) mode → TASK-028's first-order (even,0)→(2,0) coupling **generalizes beyond Mars**. **Memory:** peak RSS 1.3/4.1/7.4 GB for lmax 2/3/4 at Nrbase≤30, 10.8 GB at lmax4/Nrbase50 (heaviest physical solve); blocked rungs cost ~nothing (fail at cheap diagnostics before any coupled solve) — corroborates the TASK-021b >15 GB caution. Total wall 788 s. Artifacts `docs/figures/proposal/moon_lateral_convergence.{npz,png}`; results subsection appended to `docs/MOON_MODEL.md`. **Code/artifact commit pending PI approval** (1 new script + 2 artifacts + MOON_MODEL.md + this coord entry); this ledger update is the pre-authorized coord entry.
- `[B][2026-08-12]` **Claiming TASK-031b** (Moon lateral truncation convergence, base `67dd097`). Note for the record: I independently built a fresh TASK-031 in this clone in parallel with A's `67dd097` — same two convention decisions (degree-1 retained, C20 dropped w/ the positivity rationale + toggle) — before pulling and seeing A had already landed and A-verified the canonical version. Reset to `eca6f27` and discarded my duplicate rather than push a competing DONE; my build is backed up off-repo for diffing only. Now taking the compute follow-on that is actually B's profile. Plan per the 4-part spec: **(1) the ladder** — driver `scripts/moon_lateral_convergence.py` (driver-only, no pylov3d module touched) runs `moon_lateral.py`'s validated spectrum path at lmax=4/5/6 (7 if memory allows) at fixed Nrbase, tracking Δk20 AND the three named off-modes ((2,±2) 3.13471e-6, (2,±1) 2.76868e-6, (3,±3) 2.01884e-6), reporting step-to-step relative change and N per rung. **(2) Nrbase-independence VERIFIED not imported** — hold lmax fixed, vary Nrbase (15/30/50), since the Weber Moon's fluid outer core means Mars's radial-independence result does not transfer for free. **(3) memory watched** — 10 layers vs Mars's 4; budget for the >15 GB Mars watermark at lmax=6, report observed peak. **(4) if it doesn't converge by lmax=6–7, that is the finding** — restate whether TASK-031's Δk20=1.407e-6 survives as converged or must be qualified; no extrapolation. **Cheap extra if budget remains:** confirm the Moon's isolated (4,0) rheology channel is first-order by exponent-fitting (the Moon field has no (2,0) channel since C20 is dropped), testing whether TASK-028's ~91% Mars cancellation is Mars-specific or general. Artifacts → `docs/figures/proposal/moon_lateral_convergence.{npz,png}`; results subsection appended to `docs/MOON_MODEL.md`. Compute-heavy, token-light. Code/artifact commit pending PI approval; this claim is the pre-authorized coord entry.
- `[A][2026-08-11]` **TASK-031 pipeline committed (`67dd097`); convergence split out as TASK-031b for B.** PI reassigned 031 to B — **but B could not have finished it as it stood: the implementation was untracked on machine A, so it existed in no clone but this one.** Landing it was the prerequisite, not a formality; the ledger note said the files were "preserved in shared worktree", and committing removes any dependence on that assumption. A verified *before* landing rather than after, since B builds on it: scope clean (only MOON_MODEL.md touched besides new files, no solver module), prose standard clean, 9 tests passing — and critically **both convention decisions are pinned by tests**, including one that encodes *why* the default was chosen rather than merely asserting it. The work resolved both traps the 031 sketch named without being asked. **Split rationale:** the buildable part is done; what remains is a compute ladder, which is B's profile. The doc already states its own gap ("the first fixed-cutoff Moon lateral prediction, not a truncation-converged endpoint"), so this is a clean boundary rather than half-finished work handed over. 031b's spec explicitly warns B **not** to import the Mars Nrbase-independence result, since the Weber Moon has a fluid outer core Mars lacks, and adds a cheap test of whether TASK-028's cancellation generalises.
- `[A][2026-08-11]` **TASK-030 VERIFIED and the proposal caveat retired (`9ff7419`); TASK-033 cut for Codex.** B's ordering result reproduces exactly on A — m=0 > m=2 > m=1 at every lmax on the ladder, closest pair never nearer than 46% of the smaller shift, and the lmax=4 row matching the proposal-cited constants to **1.3e-15** (B under-claimed at ≤0.01%). The proposal's "ordering should not be relied upon" is now withdrawn rather than left standing out of inertia, with the scope kept honest: the stability result is about *ordering*, and the ~5% truncation residual still attaches to absolute amplitudes. **Codex advice — TASK-033:** its guard is genuinely good (A confirmed +11 fails / +9 passes, survives partial invocation, returns a paste-ready replacement sentence), but the exact *file* count against tolerant *test* counts means another machine's uncommitted work reds everyone's suite — currently the case, from an in-flight `test_moon_lateral.py`. That is exactly the failure the 032 spec named, so the ticket asks Codex to pick a mechanism (tolerance / git-tracked-only / warn-not-fail), **write the trade-off into the docstring as it did for the ±10**, and treats a green suite *with the in-flight files present* as the acceptance condition. The spec explicitly forbids touching those files — they are another task's work and are the very condition being tolerated. **Note on the in-flight work itself:** whoever authored `moon_lateral.py` resolved both decisions the TASK-031 spec flagged, and did it well — degree 1 removed as a centre-of-figure translation rather than a load, and C20 removed by default with the quantitative reason that retaining it drives |δμ/μ̄| to ~1.08, past the positivity domain, plus an `include_c20=True` toggle to quantify that sensitivity. That is TASK-028's lesson applied unprompted.
- `[B][2026-08-11]` **TASK-030 DONE — the m-ordering caveat retires.** Ran the diagonal (2,0)/(2,1)/(2,2) forcing solves at lmax=4/5/6 (Nrbase=30, method='combination', perturbation_order=2, via the validated `mars_lateral_love_spectrum` path — the same call `recompute_k2m_diagonal_shift` uses; `scripts/mars_m_ordering.py`, driver-only, no pylov3d module touched). The diagonal shift \|dk2m\|=\|Re k2m(lmax)−k2_uniform\| orders **m=0 > m=2 > m=1 at every lmax on the ladder** — dk2(2,0)=5.517/5.973/6.231e-5, dk2(2,2)=3.400/3.622/3.796e-5, dk2(2,1)=2.091/2.473/2.517e-5 for lmax=4/5/6. The lmax=4 row reproduces the proposal-cited `MARS_K21_FORCING`/`MARS_K22_FORCING` to ≤0.01% (validated-path regression). Each shift grows monotonically with lmax; the closest pair (m=2 vs m=1) has gap 1.31/1.15/1.28e-5 — it dips at lmax=5 but never approaches a crossing (stays ~50% of the m=1 shift). **So the proposal's "ordering should not be relied upon" is now unwarranted: m=1,2 are carried to the same lmax=6 as m=0 and the ordering is unchanged.** Scope note: this is about the *ordering* only — absolute amplitudes still carry the ~4-6% lmax-6 truncation residual and the larger Airy/crustal-model spread (TASK-027). Artifacts `docs/figures/proposal/mars_m_ordering.{npz,png}` (npz numpy-verified: ordering matrix all [0,2,1], stable=True) + `data/tests/mars/mars_m_ordering.log`; note added to MARS_MODEL.md §6 (after the TASK-027 Part-3 anchor block). Total wall ~76 min across 9 solves. **Code commit pending PI approval** (1 new script + 3 artifacts + MARS_MODEL.md §6 edit); this ledger update is the pre-authorized coord entry. Next claimable for B: TASK-015 (Mars pocoMC production) and TASK-031 (Moon lateral, M8).
- `[A][2026-08-11]` **TASK-029 VERIFIED — the new physics holds in native MATLAB, so the proposal stands.** Both controls reproduce the documented values ((4,0) 1.0001 vs 1.000; (3,0) 2.0005 vs 2.002), which is precisely what makes the new (2,0)=first-order result credible rather than an artifact of one code. Signs identical, magnitudes 0.27%/0.02%/2.48%, cancellation 91.6% vs 91.4%, and MATLAB's own `Coup` array gives 0, 0.6389, 0, 0.8571, 0, 0 across n_b=1..6 — matching Python exactly, 6/7 exact for (4,0). **The 2.48% on the net is expected, not a discrepancy:** it is a difference of two nearly-cancelling ~1.5e-5 terms, so sub-percent input differences necessarily amplify there. **The last unanchored link in the Mars chain is closed**, and the corrected proposal claim (two first-order channels, ~91% cancellation, convention-dependent net) needs no pullback. **B improved on the spec's method:** instead of extending the lateral driver, it exported the identical DWAK field to npz and had MATLAB read *that*, so both codes consume the same field rather than independently re-deriving it — a stronger test of the physics and a weaker test of the ingest, which is the right trade here since the ingest is already anchored. **TASK-032 VERIFIED** — A broke the README count deliberately, watched the guard fail, and confirmed it passes both in-suite and run alone. Suite 625 fast. Proposal merged with the latest Overleaf edits and rebuilt clean.
- `[B][2026-08-11]` **TASK-029 DONE — the two-first-order-channel result reproduces in native MATLAB, signs included.** All three deliverables anchored on the committed 4-layer model at `method='combination'`, Nrbase=30, order 2 (matching pylov3d), MATLAB R2025b. **Exponents:** (2,0)→0.9988, (4,0)→1.0001 (both first order), (3,0)→2.0005 (second) — the two controls hold, so the new (2,0)=first-order result is credible. **Signed contributions on DWAK (full amplitude):** (2,0)=−1.5858e-5, (4,0)=+1.4531e-5, both=−1.3398e-6 — **signs identical to Python (−,+,−)**, (2,0)+(4,0) linear sum matches the joint solve to 0.9%, 91.6% cancellation (Python 91.4%). **Coupling coefficients** read straight from `get_couplings`' `Coup` array match to four decimals: max|C|=0.6389 (2,0), 0.8571 (4,0), 0.0 for (1,0)/(3,0)/(5,0)/(6,0). Residual ~0.3% (isolated channels) / ~2.5% (near-zero net) is ordinary solver-level numerics at this cancellation depth; does not touch sign or the ~91% cancellation. **No proposal text needs pulling back.** Rather than extend the existing lateral driver, added two focused files: `scripts/export_mars_dwak_mu_variable.py` (read-only pylov3d use → committed npz, so MATLAB gets the identical field, not a re-derived one) and `scripts/mars_first_order_channels.m` (the anchor). Artifacts: `data/tests/mars/mars_first_order_channels.{log,mat}`; result note in `docs/MARS_MODEL.md` §9 (TASK-028 block). **Code commit pending PI approval** (npz + 2 scripts + MARS_MODEL.md); this ledger update is the pre-authorized coord entry. Next claimable for B: TASK-030 (m=1,2 diagonal solves at lmax=5,6) and TASK-031 (Moon lateral, M8).
- `[B][2026-08-11]` **Claiming TASK-029** (native-MATLAB anchor for the two first-order zonal channels — TOP PRIORITY, the only unanchored link in the Mars chain). Plan: extend `scripts/mars_lateral_cross_check.m` to (1) isolate single zonal rheology harmonics (2,0)/(4,0)/(3,0) and fit the forcing-mode k2 shift's scaling exponent in perturbation amplitude, targeting Python's 1.003/1.001/2.003 (the two controls make the new (2,0)=first-order result credible); (2) on the DWAK InSight field extrapolated to full amplitude, reproduce the *signed* single-harmonic contributions (2,0)=−1.5901e-5, (4,0)=+1.4528e-5, together=−1.3738e-6 (additive to 0.1%, 91.4% cancellation) — **signs matter more than magnitudes; if MATLAB disagrees on sign the proposal framing is wrong and gets pulled the same day**; (3) if MATLAB exposes them conveniently, confirm the coupling coefficients directly (max|C|=0.6389 for (2,0), 0.8571 for (4,0), zero for (1,0)/(3,0)/(5,0)/(6,0)). MATLAB + driver only, no pylov3d module touched; `.log`+`.mat` to `data/tests/mars/`, MATLAB version recorded; result note to `docs/MARS_MODEL.md`. Compute-heavy, token-light.
- `[A][2026-08-11]` **TASK-032 cut for Codex; A fixed the proposal-side drift directly (`d97e7bc`).** The originally-intended Codex ticket (SPDX headers for the newer modules) turned out to be **unnecessary — all 15 post-TASK-022 files already carry headers**, since the agents picked up the convention. Rather than ship busywork, A looked for real drift and found it: README claims 546 tests/39 files/521 fast against an actual **653/43/624**, and `Methods_Models.tex` still said **502** fast-lane tests plus a pinned commit of `370234d` — which *predates the anelasticity, detectability and crustal-substitution sections the document now describes*, so a reader checking out the pin would not have found them. A corrected the proposal side (counts + pin → `4dbaf03`) since that repo is outside Codex's allowlist. **The Codex ticket is deliberately weighted toward the guard, not the fix:** this is the fourth manual count correction, every one reactive, so the deliverable is a test that fails when the README stops matching reality. Spec names the two traps rather than leaving them to be discovered — a test that recomputes and self-compares proves nothing, and `request.session.items` reflects the *current invocation*, so the guard would fail spuriously when its own file is run alone — and it explicitly permits shipping the file-count check alone if the full guard cannot be made robust, on the grounds that a reliable partial guard beats a flaky complete one.
- `[A][2026-08-11]` **Punch list cut for B: TASK-029 (top), 030, 031 — all claimable now, none blocked on A.** Rationale for the ordering. **029 is the priority because it is the only unanchored link left in the Mars chain**: every other Mars result has a native-MATLAB cross-check at 7e-12 or better, but the two-first-order-channel result and its 91% cancellation rest solely on the Python port plus a coupling-coefficient inspection — and they are cited in the proposal, having *changed* a headline claim. The controls matter as much as the new number: reproducing (4,0)→1.000 and (3,0)→2.002 in MATLAB is what makes (2,0)→1.003 credible. **If MATLAB disagrees on sign, the proposal text must be pulled back the same day** — that is stated in the spec. **030 retires a live proposal caveat** (m-ordering unreliable because m=1,2 were never run above lmax=4 while m=0 reached lmax=6) and is pure compute. **031 opens M8** now that Moon data and loaders have both landed; the spec warns B off mirroring the Mars code on two specific decisions — the Moon's ~1.9 km degree-1 offset, far larger than Mars's, and the degree-2 convention that TASK-028 showed is a first-order lever, not a bookkeeping detail. A-side queue while B works: nothing blocking; the density/calibration axis remains structurally out of reach (|δμ/μ̄| already 0.969 against the positivity bound of 1.0), so it needs a non-linearized formulation rather than more compute.
- `[A][2026-08-11]` **TASK-028 DONE (`196670c`); proposal corrected (`1926058`). The review found the finding A had stopped one step short of.** A discovered that (2,0) is a first-order channel; **the review found that it enters with the OPPOSITE SIGN and cancels 91.4% of the (4,0) result** — (2,0) −1.5901e-5 vs (4,0) +1.4528e-5, net −1.3738e-6, singles summing to the pair within 0.1% as first order requires. So the proposal's "single most significant scientific finding" is convention-dependent: (4,0)'s +1.75e-5 stands alone only because Airy zeroes C20; a measured crustal field nearly annihilates it. Mechanism robust, net amplitude not. **Three errors were A's own and are named in the record:** (i) a **memory-written author list in SOURCES.md containing a nonexistent author** ("Kim, J.", a corruption of King, S. D.) — the exact rule A has been enforcing on every agent, corrected in `bb8c1b2`-style with the error kept in-file; (ii) quoting ×1.22–1.25 as crustal sensitivity when that is the *off-diagonal* spread, not a (2,0) statistic — correct figures ×1.551 shipped / **×1.379 like-for-like**; (iii) Airy-validation numbers computed on a C20-stripped field rather than the shipped one (0.984–0.986 → **0.952–0.966**). **A's one good instinct held:** flagging to the reviewer that selecting all five at ρ=2900/~50 km might suppress the spread — it does (2% of the source's published 30–72 km range), so **×7.6 stays** and now carries this as a caveat rather than being displaced. **Two process wins worth keeping:** the fix agent caught that its *own first* mutation fix was a near-miss (an equivalence test between two callers of the *same* function object cannot detect that function being mutated) and replaced it with a hand-computed literal pin — A re-verified independently that it kills the 1/√2 mutant that all 31 prior tests missed; and it declined to bank two numbers it could not reproduce, labelling them unverified rather than shipping them. **Standing lesson reinforced: finding a mechanism is not the same as knowing what it does — always ask what a newly discovered term does to the terms already claimed.**
- `[A][2026-08-11]` **TASK-028 complete on A's side — four findings, three of which touch published proposal text. Under adversarial review before anything moves.** **(1) C20 split resolved** (DWAK, lmax=4/Nrbase=30): Airy 5.5174e-5; C20-suppressed 6.4598e-5; as-shipped 4.7689e-5 → **crustal pattern alone ×1.17, C20 convention alone ×0.74, combined ×0.86** — the two effects partially cancel, so the naive shipped-vs-Airy comparison *understates both*. The 0.58–0.90× reduction flagged earlier is therefore dominated by the C20 convention, not by the crustal pattern; the pattern actually pushes the forcing shift slightly *up*. **(2) NEW SCIENCE, and it contradicts published text: (2,0) rheology is a FIRST-ORDER channel into the (2,0) forcing mode.** Measured scaling exponent **1.003**, with controls reproducing the documented values exactly — (4,0) → 1.001 (doc says 1.000) and (3,0) → 2.003 (doc says 2.002). So `docs/MARS_MODEL.md` §5 and `Methods_Models.tex` §4.5 are **wrong** where they call (4,0) "the one rheology degree in the n_lv<=4 set for which this is possible for a degree-2 zonal tide" — (2,0) qualifies too (parity 2+2+2=6, even, spheroidal). The Airy path never exercises it only because it drops C20 by construction; a measured Moho does not. **(3) Airy pattern validated** — dt correlation 0.984–0.986, off-modes within ±16–28%. **(4) The ×7.6 crustal-uncertainty headline now in the proposal looks wrong** — five independent seismically-calibrated models give ×1.22–1.25, comparable to truncation's ×1.13. **A flagged the obvious counter-argument to the reviewer rather than banking the result: all five share gravity data, inversion method, and were *selected* at ρ=2900 and ~50 km, which may artificially suppress the spread** — if so ×1.22–1.25 understates the true sensitivity and must not simply replace ×7.6. Review is also tasked to adjudicate the C20 retention argument itself (is `(R_moho/R_topo)²·C20_topo` the right level-surface comparator, or should it be a Radau-type relation?), since retaining C20 is exactly what activates finding (2). 31 tests pass; module still uncommitted pending the verdict and the user gate.
- `[A][2026-08-11]` **TASK-028 first results — the Airy pattern is VINDICATED, and the ×7.6 error-budget headline turns out to be wrong.** Five InSight-calibrated Moho models (all ρ=2900, mean thickness within 1 km of the 50 km reference, so pattern-only comparison) run through the validated coupled path at lmax=4/Nrbase=30. **(1) Pattern agreement is excellent:** the underlying dt fields correlate with the Airy field at **0.984–0.986** and match its rms within ±17%; the off-diagonal response modes land within ±16% of Airy — (3,0) 6.96–8.47e-5 vs Airy 7.29e-5, (2,±2) 3.46–4.29e-5 vs 3.81e-5, (3,±1) 2.36–2.94e-5 vs 2.35e-5. **(2) The real crustal-model uncertainty is ×1.22–1.25, not ×7.6.** TASK-027's ×7.6 came from sweeping crust/mantle densities across their full published ranges — a far wider excursion than reality permits. Swapping in five genuinely independent, seismically calibrated crustal models moves the modes only ×1.22–1.25, i.e. **comparable to the truncation ladder's ×1.13, not sevenfold larger**. The proposal currently states ×7.6 as the operative uncertainty and that **must be corrected** — it is an upper bound from an unphysical sweep, not the measured crustal-model sensitivity. **(3) Open:** the (2,0) forcing shift behaves differently from the off-modes, dropping to 0.58–0.90× of Airy (spread ×1.55 across models) — and that is the quantity the k2m splitting and detectability ratios rest on. Candidate causes are the crustal pattern and the C20-convention difference (the InSight loader **retains** C20, worth 19–26% of field power, and a (2,0) rheology harmonic couples to the (2,0) forcing at *first* order, so Airy structurally cannot have it). A C20-suppressed control solve is running to separate them; nothing goes near the proposal until it does. **A's independent check of the C20 argument holds:** Moho flattening is 0.355–0.474× the level-surface prediction consistently across all five models, so the decoupling the build agent argued for is real. 31 tests pass.
- `[A][2026-08-11]` **TASK-028 cut and dispatched — attacking the dominant error term at last.** Data fetched and committed (`c2a8dbe`): five InSight-calibrated crust-mantle-interface SH models from the Wieczorek et al. (2022) archive (Zenodo 6477509, CC-BY-4.0, lmax=90). Fetch notes worth keeping: the archive is a single 2.7 GB tarball, so it was **streamed and only `constant/` extracted** rather than stored whole; the files turn out to be `n,m,Clm,Slm` comma-delimited in the same convention as MarsTopo719, so `load_shape` reads them unmodified — **Codex's TASK-023 delimiter work paid off immediately**. Selection is like-for-like by construction: all five at crust density 2900 (matching `MARS["crust_density"]`) and mean thickness 49.59–50.48 km against the project's 50 km reference, so a substitution moves the lateral *pattern* while holding mean and density fixed, and the five span distinct interior models so their spread is an interior-model sensitivity rather than one alternative. A sixth candidate was fetched and **deliberately dropped — byte-identical sha256 to another**, which would have double-weighted that model in the spread. Build agent briefed that the subtlest call is whether to strip C20 from a Moho-derived thickness (the Airy path drops it as rotational flattening, but the Moho has its own flattening) and told to argue the choice rather than mirror the Airy code, and that a null result is equally publishable.
- `[A][2026-08-11]` **PI reversed the lmax=6 decision; proposal back to the anchored lmax=4 (`0e6c773`).** The PI's read — that lmax=6 introduced unacceptable uncertainty — is correct, and the reason is sharper than "more uncertainty": **quoting lmax=6 did not reduce the uncertainty, it made it impossible to state cleanly.** Three costs compounded: (i) only m=0 and three off-diagonal modes were ever run at lmax=6, so the quoted set **mixed truncations across both orders and modes** and stopped being internally comparable; (ii) lmax=6 has **no native cross-check** — the 7e-12 MATLAB agreement is an lmax=4 result — so it forfeited the validation anchor the whole Mars chain rests on; (iii) it **did not settle the question it was meant to settle**, since m=1,2 were never rerun and the ordering caveat survived unchanged. Everything is now quoted at lmax=4 (one truncation, mutually comparable, fully anchored) with the cutoff effect reported as what it is — a **measured, signed, mode-dependent bias** ((3,0) +17%, (2,±2) ~0%, (3,±1) −34%, Δk20 +13%). A reader can apply those shifts to the anchored values; they could not have reconstructed the anchored values from a partially converged table. **Also corrected a framing error of mine:** the section now states which effect dominates, because the earlier text invited the wrong conclusion — crustal-model spread ×7.6 against the truncation ladder's ×1.13, so refining the cutoff chases the sub-dominant term. **Standing lesson: "better converged" is not automatically "better to quote" — internal consistency and a validation anchor can outweigh a known, measurable bias.** Open for A: the non-Airy InSight-crust substitution, which is the dominant term.
- `[A][2026-08-11]` **PI decision implemented: proposal now quotes lmax=6** (SSS_2025_Mars `7fbc338`). Detectability restated — Δk20 6.23e-5 → **7.2× JPL / 37× GSFC** (was 8.2/41); off-diagonal (3,0) **24–57×** (was 28–66), (2,±2) 38×, (3,±1) 95×; proposal body ratio 8–41× → **7–37×**. **Two costs of this choice are now stated at the point the numbers appear, not buried:** (i) the orders are no longer at a common truncation — only m=0 reached lmax=6, so m=1,2 stay at lmax=4 and the three are not mutually comparable, which promotes the m-ordering caveat from precautionary to load-bearing; (ii) the quoted configuration is the one *without* the native cross-check, since the 7e-12 MATLAB agreement is an lmax=4 result — the text now says explicitly which values are anchored, which are converged, and that the listed shifts let a reader recover the anchored pair. The less-significant off-diagonal modes were only ever run at lmax=4, so the ~390× tail is labelled as such rather than presented as uniform with the converged three. Residual ~5% truncation uncertainty attached, since lmax=6 is approached-not-reached. **Also done:** figures regrouped per the PI — the rigidity map now pairs with the published Shadab infiltration map (colour scale reversed to RdBu so red is the low end, matching that panel's sense), and fig:ai drops to inference + tidal signature; both regenerable (`fig_mars_rigidity_percolation.py` new, `fig_composite_inference_structure.py` reduced). **Open, deferred to A by PI decision:** the non-Airy InSight-crust substitution and its data fetch — which matters, since TASK-027 showed crustal-model uncertainty exceeds truncation by ~7×, so it is where the remaining error actually lives.
- `[B][2026-08-11]` **PI decisions on the three TASK-027 follow-ups A surfaced at VERIFIED.** (1) **Quote lmax=6** — the proposal takes the better-converged (lmax=6) numbers over the lmax=4 pair that carries the 7e-12 MATLAB anchor. Consequence: TASK-026's proposal detectability table must be re-stated to the per-mode *converged* values A already computed at verification — (3,0) 28.5→**24.4**, (2,±2) 38.6→**38.1**, (3,±1) 62.4→**95.0**, Δk20 8.2→**7.2** JPL / 41.3→**36.6** GSFC — replacing the single blanket truncation caveat with per-mode values (A's "wrong instrument" finding). **This edit lands in TASK-026 territory (`docs/MARS_MODEL.md` detectability section + `Methods_Models.tex`), so it is handed to A, not done by B.** Note the m=1,2 anchor stays at lmax=4 (never re-run higher), so the m-ordering caveat is unchanged and must survive the rewrite. (2) **Airy second pass (non-Airy InSight-crust substitution) — deferred** (not scheduled now). (3) **InSight-crust data fetch — confirmed deferred to A.** No B task open; TASK-027 remains closed (DONE→VERIFIED).
- `[A][2026-08-11]` **TASK-027 VERIFIED — and it resolves the last unanchored link, while sharpening one proposal number and worsening another.** All of B's numbers reproduce on A from the committed artifacts. **The headline win: the m=1,2 diagonal solves now have a native-MATLAB anchor, matching to 7.0e-12** — TASK-026's proposal-cited k2m splitting is no longer resting on unvalidated solves, closing the gap A flagged when cutting part 3. Nrbase-independence is *verified* (4.5e-8 across 15/30/50), so B's angular-ladder-at-modest-Nrbase methodology is sound rather than assumed. **The finding that matters most is one B reported but did not follow through:** truncation error is strongly mode-dependent, so it propagates unevenly into the detectability table now in the proposal — (3,0) is *understated* 17% at lmax=4 (ratio 28.5→24.4, i.e. the headline requirement is easier than published), (2,±2) is stable at ~1%, but **(3,±1) is overstated 52%** (62.4→95.0). Corrections in both directions means the proposal's single truncation caveat is the wrong instrument and should be replaced with per-mode converged values. **Second-order but real:** convergence is *not* reached by lmax=6 (steps +8.26%, +4.32% — shrinking, monotone, still ~4%/degree), and **m=1,2 were never re-run above lmax=4**, so the m-ordering caveat in the proposal stands unchanged. **Scientifically the bigger point, which B did state:** the Airy crustal-parameter spread is ×7.6 against truncation's ×1.13 — **crustal-model uncertainty dominates truncation by ~7×**, so further lmax work is not where the remaining error lives. A judgment call now belongs to the PI: the lmax=4 numbers carry the 7e-12 MATLAB anchor, the lmax=6 numbers are better converged but unanchored — which pair a NASA proposal should quote is a decision, not a computation. Code/doc commit held at the user gate per standing rule.
- `[B][2026-08-10]` **TASK-027 DONE — awaiting VERIFIED by a different (Opus-tier) driver.** All three parts complete; results in `docs/MARS_MODEL.md` "Lateral variations" subsection 6 (rewritten from the old lmax=5 spot check) and the TASK-026 tier-1 truncation caveat updated. Artifacts: driver `scripts/mars_lateral_robustness.py`, `docs/figures/proposal/mars_lateral_robustness.{npz,png}`, extended `scripts/mars_lateral_cross_check.m` + `data/tests/mars/mars_lateral_cross_check.{mat,_k2m.log}`. **(1) Truncation convergence** — lmax=4/5/6 at fixed Nrbase=30, N=115/163/219; (2,0) forcing shift 5.5174e-5→5.9732e-5 (+8.26%)→6.2311e-5 (+4.32%): a *converging* sequence (steps shrink, monotone up) but **not flat by lmax=6** — still ~4%/degree, so the absolute shift is only ~4-6%-converged. lmax=4 reproduces A's 5.517e-5, lmax=5 A's 5.973e-5 exactly. Off-modes: (3,0)/(2,±2) settle to ≤1.4% steps by lmax=6, but **(3,±1) is misestimated at lmax=4** (−33% at lmax→5), so (3,±1) needs lmax≥5. **Nrbase-independence verified, not assumed**: Nrbase=15/30/50 at lmax=4 identical to <0.005% on every mode — the angular ladder at Nrbase=30 is legitimate (peak RAM ~11 GB at lmax=6/Nrbase=30, under the 021b 15 GB watermark). **(2) Airy sensitivity** — the lateral field is exactly linear in `AIRY_FACTOR`; swept rho_crust∈{2700,2900,3100}/rho_mantle∈{3400,3500} (AF 3.375–10.333): (2,0) shift ranges 2.23e-5–1.69e-4 (×7.6, super-linear at high AF from the order-2 self-term), off-modes 130–164% of mean. **Crustal-model uncertainty dominates truncation uncertainty by >10×.** Non-Airy InSight-crust substitution (Part 2 second pass) needs a data fetch — deferred to A per the spec. **(3) MATLAB m=1,2 cross-check** — native LOV3D on the committed model+`mu_variable` for m=0/1/2: (2,1)=0.169020913466 and (2,2)=0.169033995416 match `MARS_K21_FORCING`/`MARS_K22_FORCING` to **~7e-12** ((2,0) to 1.5e-7, limited only by the 7-fig Python ref). The proposal-cited diagonals now have their native anchor **at lmax=4** — but Part 3 did *not* re-run m=1,2 at higher lmax, so the m-ordering convergence (TASK-026 caveat) stays open. **Do §5's conclusions survive?** The (4,0)-driven first-order shift survives qualitatively (real, grows with lmax, present in MATLAB, lmax=4 under-states it); a *converged absolute amplitude* does not — ~4-6% truncation + factor-several Airy uncertainty. Fast lane green (591 passed, 28 deselected). Code/script/doc commit awaiting user approval (only the coord claim was pre-authorized).
- `[B][2026-08-11]` **Claiming TASK-027** (Mars lateral robustness). Plan per the 3-part spec: **(1) truncation convergence** — driver `scripts/mars_lateral_robustness.py` runs `mars_lateral_love_spectrum` at lmax=4,5,6(,7 if ≤~20 GB) tracking the (2,0) forcing k2 shift AND the top off-modes ((3,0),(2,±2),(3,±1)) step-to-step, at fixed modest Nrbase; the Nrbase-independence claim (021b's argument) is *verified* by holding lmax fixed and varying Nrbase=15/30/50 at least once, not assumed. Directly settles A's TASK-026 REJECT finding that lmax 2→4 moved the splittings 24–79% and reversed the split ordering. **(2) Airy sensitivity** — sweep `rho_crust`/`rho_upper_mantle` (→ `AIRY_FACTOR`) across defensible published Mars ranges, report induced spread in the (2,0) shift + top off-modes (analogue of TASK-021's reference-crust table); non-Airy InSight-crust substitution deferred to A if the model isn't offline. **(3) MATLAB m=1,2 cross-check** — extend `scripts/mars_lateral_cross_check.m` to run forcing (2,1)/(2,2) on the committed model+`mu_variable` field, confirm `MARS_K21_FORCING`/`MARS_K22_FORCING` (the proposal-cited 8.2×/12.0×/8.2× ratios rest on these two currently-unanchored solves), save `.log`+`.mat` to `data/tests/mars/`. No pylov3d/solver module modified — driver scripts only. Artifacts → `docs/figures/proposal/mars_lateral_robustness.{npz,png}`; results section in `docs/MARS_MODEL.md`. Compute-heavy, token-light.
- `[A][2026-08-11]` **TASK-023 VERIFIED.** Codex's Moon-loader work checks out on inspection: the SPA geographic test is a genuine 4.2%-of-surface constraint (minimum 781 km from the published basin centre), the delimiter sniffing keeps Mars loading unchanged while removing the need for any future fetch to manufacture a reformatted shape file, and the malformed-input guard still errors loudly instead of silently misparsing. **New standing rule from this review: Codex cannot reach the network, so any citation it writes is from memory and MUST be verified by A before shipping.** This one happened to be exactly right (Garrick-Bethell & Zuber 2009, Icarus 204, 399-408, SPA at 53S/169W), but the project has already been burned once by an unverifiable citation, and "it was correct this time" is not a process. Add the rule to future Codex specs.
- `[A][2026-08-11]` **TASK-026 fix round complete (`6cd7a51`), TASK-023 landed from Codex (`bb71493`); 591 tests green.** All 12 review findings applied, and the fix agent independently re-derived the load-bearing ones rather than taking the review on trust — including catching a subtlety the review missed: the basis correction must key on the spectrum's *actual* forcing order, not the optimistic bound's hypothetical (2,2) geometry, or (3,0) would spuriously shift. It also pushed back with evidence on two points (wall-time, and the GRAIL Stokes-space conversion it could only reproduce to order-of-magnitude) rather than asserting numbers it could not reproduce — recorded as prose caveats, not shipped values. **The correction record is now the useful artifact**: docs/MARS_MODEL.md retains every retracted claim with its retraction, and the tier-1 diagonal comparison is explicitly distinguished from the tier-2 category error rather than both being silently "fixed". **Standing lesson, logged for future tickets: a suite that pins only the case immune to the likeliest error class is not coverage** — all 41 tests passed against a √2 error because the sole pinned off-diagonal mode was the m=0 one. Remaining Mars-lateral exposure is now concentrated in TASK-027 (B): the lmax convergence that the proposal-cited k2m ordering depends on, plus the m=1,2 MATLAB cross-check.
- `[A][2026-08-11]` **TASK-026 adversarial verification: REJECT — and one finding was live in the pushed proposal, now corrected (`e12425c`).** The diagonal derivation, the diagonal k2m numbers (reproduced exactly; radial grid converged to 5e-14; zero-amplitude coupled run confirms 0.169 is the right baseline for all three m), and every citation actually used check out. What failed: **(1) BLOCKER — a √2 basis-normalization error** on every m≠0 response mode. LOV3D's SH basis is not uniformly normalized across m (`src/get_map.m` ~196-201: Y_n^0 has norm 1, Y_n^{±m} has 1/√2), so an off-diagonal k straddling m=0 is a ratio of coefficients in differently-normed basis elements. Confirmed two ways, including a reciprocity check (k[(2,±2)←(2,0)]=3.807e-5 vs k[(2,0)←(2,2)]=3.807e-5, equal only if the real-basis admittance carries the √2). Tail ratio 550x→388x; **the (3,0) headline is unaffected (m=0)**. Damningly, all 41 tests passed because the only mode pinned was (3,0) — the one mode the error cannot touch. **(2) `ratio_grail` commits the exact diagonal/off-diagonal conflation the same document forbids**, comparing an off-diagonal k against GRAIL's diagonal σ(k3); done properly it is 43-55x, not 28.8x, and the "both benchmarks agree within 1%" claim is an artifact. **(3) The Moon-vs-Mars comparison was the real proposal risk:** Love-number space says 8-12x, but ξ2 for Earth-on-Moon is 1.50e-6 against 2.04e-9 for Sun-on-Mars — a factor **736** — so the *instrumental* requirement is ~6000-8800x, i.e. ~3.8 orders of magnitude, not one. **(4) Williams et al. (2014) WAS retrievable** (the shipped text claimed HTTP 402; our own MOON_MODEL.md already cites it) and carries a second GSFC analysis of the same GRAIL data giving 41/16/12x — and the two analyses disagree on k22 by 9.1e-4, **27x the splitting being predicted**, so real reproducibility is ~1e-3 not 2.5-4.5e-4. **(5) An unverified k3=0.0163±0.0007 attributed to Williams from web-search summaries** — the paper says 0.0089±0.0021; exactly the banned practice, shipped in prose. **(6) lmax not converged:** lmax 2→4 moves the splittings 24-79% and *reverses* which order splits most; lmax=5 untested — TASK-026 shipped proposal-cited numbers ahead of TASK-027, which is scoped to settle precisely this. **Lesson: a test suite that pins only the mode immune to the likeliest error class is not coverage.** Fix round dispatched.
- `[A][2026-08-11]` **Three tracks moving.** (1) **TASK-026 under adversarial Opus verification** — it is already cited in the proposal and its technical core (the |k_nm| → time-varying Stokes coefficient relation) has **no MATLAB anchor**, unlike everything else in the Mars chain, so it gets scrutiny proportional to that exposure; the review is specifically tasked to re-derive the relation from first principles and to attack the off-diagonal generalization and the fairness of benchmarking a Mars prediction against a lunar achieved precision. (2) **Anelasticity is being folded into `Methods_Models.tex`** (Sonnet) — TASK-025a/b were committed and documented in-repo but never reached the proposal's traceability document, which currently admits the gap in a parenthetical; the port carries A's TASK-025b correction (Q reduces the degeneracy 2.69x rather than not breaking it; alpha=0.286 is weakly informed and must not be presented as a measurement). (3) **TASK-027 part 3 added for B:** MATLAB cross-check of the m=1,2 diagonal forcing solves, since the proposal now quotes ratios derived from two solves that have no native-MATLAB anchor while the m=0 spectrum does.
- `[A][2026-08-10]` **TASK-026 DONE (`cb67b1e`) — and the build agent caught a physics error in MY redirect, correctly.** I told it the off-(2,0) spectrum's (2,±2) mode *is* the predicted k22 splitting. It is not: that mode is an **off-diagonal** coupling response (forced at (2,0), measured at (2,2)), whereas the k2m splitting GRAIL/MaQuIs discuss is the **diagonal** admittance (forced and measured at the same (2,m)). The agent refused the framing, ran fresh coupled solves at forcing (2,1)/(2,2) to get the diagonal quantities properly, and reported both tiers separately. It also declined to take my handed-over MaQuIs text extract on faith — re-fetched the paper itself — which is exactly right and is the behavior the anti-fabrication rule is meant to produce. **Lesson for future redirects: a coordinator-supplied "retrieved" source is still hearsay to the receiving agent; keep instructing them to verify, and expect them to correct me.** Headline: neither observable detectable today, but the diagonal splitting is only ~8-12x beyond GRAIL's *demonstrated* precision at an easier body — a measurement requirement, not a closed door, and the tighter channel by far. My conflation had already propagated into `Methods_Models.tex`; corrected there with the distinction spelled out and the error recorded in-text. Proposal now carries a new §4.8 "Detectability of the off-forcing observables" with the numbers, plus fig8.
- `[A][2026-08-10]` **TASK-026 scope resolved by the PI, and it is a much stronger result than the generic version.** Benchmark mission is **MaQuIs** (Wörner et al. 2023, *Planet. Space Sci.* 239, 105800, doi:10.1016/j.pss.2023.105800) — a GRACE/GRAIL-heritage dedicated Mars gravity mission using optical/quantum inter-satellite ranging. Open-access full text retrieved (ScienceDirect 403s; the UvA Pure copy at pure.uva.nl/ws/files/155114798/ is fetchable) and handed to the build agent as a pdftotext extract. **The key linkage:** MaQuIs's own Love-number discussion notes that separate k20/k21/k22 were determined for the Moon from GRAIL (Williams et al. 2014), that their small order-to-order differences "may be relevant in processing of high-accuracy data proposed here", and that past attempts to measure these separate coefficients at Mars via Doppler tracking **proved unsuccessful**. That order-splitting of k2m *is* the degree-2 part of our off-(2,0) spectrum — our (2,±2)=3.81e-5 and (2,±1)=3.79e-6 are precisely the predicted departures from k20 induced by lateral crustal structure, and TASK-021 already showed the splitting (not k2 itself) is what carries front-location information. So TASK-026 is no longer "is this exotic spectrum measurable?" but "our validated model predicts the size of a splitting GRAIL already measured at the Moon, MaQuIs proposes to enable at Mars, and Doppler tracking has never resolved there." Build redirected accordingly: lead with degree-2 splitting benchmarked against GRAIL's achieved k2m uncertainties, higher-degree modes as a second, new-observable tier. MaQuIs quantitative anchors for the comparison line: current Mars field d/o 90-100; MaQuIs targets >degree 90 (~115 km) and up to 360 d/o for crustal structure; seasonal polar signal ~230 μGal at 150-200 km altitude. Paper also flags k2/Q frequency dependence as an interior constraint (Pou et al. 2022; Dirkx et al. 2014) — the channel TASK-025a/b built.
- `[A][2026-08-10]` **Mars lateral status review for the proposal.** What is solid: the Airy→rigidity→coupled-spectrum pipeline is MATLAB-validated to 2.95e-13 (N=115 exact, TASK-014 pt2), the (4,0)↔(2,0) first-order mechanism is verified against the coupling coefficients themselves, 14 tests, fig6 shipped. **What is missing, ranked by proposal impact:** (1) **off-(2,0) detectability is unassessed** — the traceability doc defers it in writing twice, and it is exactly what RQ1's "detectable signatures" claim needs; largest off-mode (3,0)=7.29e-5, 21 modes above 1e-6 → **TASK-026 cut, build dispatched**. (2) truncation shown at one step only (8.3% at lmax=5, not convergence) and Airy is the sole crustal model despite §5 flagging the (4,0) result as ~1:1 sensitive to the Airy calibration → **TASK-027 cut for B**. (3) lateral is forward-only — not wired into `mars_mc`/`forward`, so the "digital twin" inference loop does not yet include lateral parameters (design needed before ticketing). (4) the lateral spectrum is elastic-only; now that TASK-025a/b exist, off-mode phase lags are an unexplored discriminant. (5) `couplings.py` ST-discarding mode-label defect still open (labels only, not values). Note for the PI: TASK-026 carries a scoping question — which measurement scenarios to benchmark against — defaulted to current-Mars-tracking and GRAIL-class, flagged rather than silently chosen.
- `[A][2026-08-10]` **TASK-025b VERIFIED, with one headline correction.** All four chains re-analyzed independently on A — every B-reported number reproduces, and Maxwell−Q recovering the exact TASK-019 elastic `mu_scale`=0.9650 is a strong validation that the new 6-param sampler is consistent with the shipped 4-param one. Driver likelihood read and sound. **Correction:** B's "Q does not break the degeneracy" overstates the negative. Q leaves mu_scale's *location* unmoved and the mu–alpha ridge intact (both true, both the reason the k2 gap still cannot be partitioned), but it **tightens mu_scale 2.69× and log10_eta 1.87×, and flips the sign of corr(mu_scale, log10_eta) from −0.666 to +0.510** — Q is informative about rigidity, just not decisive. The defensible claim for any write-up is "Q substantially reduces but does not eliminate the degeneracy". alpha=0.286 is only 1.09× tighter than its prior-driven width, so it remains weakly informed — do not present it as a measurement. **Proposal note:** with 025b in hand, the anelasticity chain is quotable, but the quotable statement is the *identifiability* one (Maxwell is excluded at the full-posterior level; Andrade is admissible; rigidity and rheology remain entangled), not a rigidity or alpha determination.
- `[B][2026-08-10]` **TASK-025b DONE — driver + docs + artifacts committed; awaiting VERIFIED.** Ran spec option (c): the full 2×2 matrix {Maxwell,Andrade}×{+Q,−Q}, each converged to the TASK-019 standard (n_active=64, n_effective=128, dynamic; Kish ESS>4000; 0 solver failures). **The three degeneracy questions, in numbers:** (1) *Degeneracy broken?* **No.** Andrade −Q→+Q leaves `mu_scale` unmoved (1.007→1.012, well within 1σ) and the `mu_scale`–alpha anticorrelation strong (−0.673→−0.697); Q only halves the viscosity width (log10_eta 20.4±1.0→20.3±0.5) and sharpens alpha. Q selects among Andrade models but does not partition the k2 gap between rigidity and rheology. (2) *`mu_scale` vs elastic 0.965:* moves **up to 1.012** (+4.9%) — adding Andrade dissipation returns the mantle to ~as-built rigidity, since anelasticity now supplies the k2 enhancement mu-softening supplied elastically. Crosses bodies (elastic on real 10-layer, Andrade on PyALMA3 simplified); ~1/5 of the shift is the +1.1% simplified-vs-real elastic-k2 structural offset (quantified from 025a; unremovable — no real-body Andrade path). (3) *Preferred alpha:* **0.286 (+0.066/−0.052)**, top of Efroimsky's 0.2–0.3 band, informative vs the 0.2–0.4 prior but not sharp. **Negative control:** Maxwell +Q infeasible against all 5 observables (rails mu=1.78, R_fluid_core=459 km ceiling, eta=1e19 floor; still lands k2=0.0141 vs 0.0242 and Q=233 vs 38) — the full-posterior form of 025a's "Maxwell → Q≈0.79" finding; Maxwell −Q recovers the elastic TASK-019 answer exactly (mu=0.965, k2=0.0242, eta unidentified→Q≈15000 negligible). Chains+pairplots at `docs/figures/proposal/moon_anelastic_{chain,pairplot}_{maxwell,maxwell_noq,andrade,andrade_noq}.{npz,png}` (per TASK-021b artifact precedent). Driver `scripts/moon_anelastic_pocomc.py` uses a custom 5/6-param log-posterior (forward.log_likelihood compares Re(k2) only; Q is an Im(k2) constraint) and makes the Andrade simplified body θ-aware so mu_scale flows into k2/Q. No anelastic module modified. Fast lane green. **Note for VERIFIER: headline is a *negative* identifiability result (degeneracy survives Q), and the mu_scale comparison crosses bodies (real elastic vs simplified Andrade) — read question 2's caveat.**
- `[B][2026-08-10]` **Claiming TASK-025b** (joint rigidity/viscosity/alpha Moon fit — the anelastic successor to TASK-019). Plan per spec option (c): (a) Maxwell-only joint fit via native pylov3d on the real 10-layer Weber Moon as a validated smoke test (expected to hit the Q-prior edge, since 025a shows Maxwell → Q≈0.79); then (b) the Andrade science run via PyALMA3 on the simplified body, quantifying the structure penalty first from 025a's own Maxwell-real-vs-simplified control (0.19% on Q). Free params = existing 4 + eta (log-uniform) + alpha (uniform), ranges taken verbatim from 025a's "Literature parameter ranges"; 5th observable Q=38±4 at the draconic month using the constant in anelastic_moon (not hardcoded). Convergence to TASK-019 standard; chain → `docs/figures/proposal/moon_anelastic_chain.npz` + pairplot. Reports the three degeneracy questions in numbers. No anelastic module modified beyond driver needs. Compute-heavy, token-light.
- `[A][2026-08-10]` **Three tracks committed at the user gate: `c4bb06e` (TASK-022 SPDX headers, 86 files), `0ac6a0f` (TASK-024 Moon field data), `f96cef9` (TASK-025a anelasticity).** Fast lane 521 / full 546 / 39 files. **TASK-025b is now claimable by B** — the joint (mu_scale, eta, alpha) Moon fit that resolves the rigidity/anelasticity degeneracy 025a could only flag: k2 alone cannot separate them (elastic mu_scale≈0.955-0.96 closes the same 4.6% gap with zero anelasticity, and the shipped TASK-019 posterior already does), so 025b adds Q=38±4 at the draconic month as a 5th constraint and asks whether that breaks the degeneracy, where mu_scale lands vs the elastic 0.965, and what alpha the data actually prefers. **TASK-023 also claimable by Codex** (Moon loader hardening + tests; data landed). Remaining A-side queue: Moon lateral stage (M8) once 023 lands.
- `[A][2026-08-08]` **TASK-025a citation re-verification: CLEAN (10 minor fixes, all applied by A).** A second Opus pass re-retrieved every source independently (pdftotext on primaries, not the fix agent's leftovers) and confirmed the fabricated Walterová quote is gone and its replacement is **verbatim-exact**; every other retrievable quotation matched exactly; every flagged claim (draconic-vs-anomalistic month, Nimmo's actual conclusion, the alpha=0.08 Model-1 rejection, Bills 85.58±0.37 vs Lainey 79.91±0.69, Pou L., Konopliv 2020 values) verified correct; no numeric value differs between the two docs or between doc and code. Applied since: Bagheri et al. (2022) is *Adv. Geophys.* **63, 231-320** (not 66); "Lainey et al. 2021"→2020; a "≤"-for-"≲" glyph alteration inside a verbatim quote restored and its scope narrowed to the follow-up studies Bagheri actually cites; Efroimsky (2012) section 5.4→**5.3**; the Dumoulin quote's "fetched directly" assurance downgraded to a stated paraphrase-grade caveat (no PDF route was retrievable on re-check); the ΔJ2/ΔC22/ΔS22 statement re-attributed from Williams & Boggs (2015) to **Williams et al. (2014)**, where it was verified verbatim; "independently corroborates" removed for Bagheri 2022 (Efroimsky is a co-author of both sources — not independent); **Tan & Harada (2021), *Icarus* 365, 114361 restored** under its journal reference (the paper is real; only the arXiv id an earlier draft used was wrong, and it belongs to Briaud et al. 2023, now cited as a direct corroboration); Khan et al. (2014) given its full reference. 30/30 anelastic tests green after the edits. **025a is gate-ready.**
- `[A][2026-08-08]` **TASK-025a Opus adversarial review: REJECT — on citations, not physics.** The science survived every attack: Maxwell-only claim confirmed (incl. vendored MATLAB), Maxwell↔PyALMA3 agreement reproduced to the printed digits, mutation testing shows the 1e-3 tolerance discriminates (sign flip / dropped 2π / factor-2 each fail 6-11 tests), and **the structure-artifact concern I flagged resolved in the builder's favor**: Maxwell on the real compressible 10-layer body vs the simplified incompressible one moves Q by 0.19%, against a Maxwell/Andrade contrast of ~47x — so 0.79-vs-36.9 is a rheology result, not a structure artifact. Nrbase-converged. **But the sourcing failed hard, and one failure is the project's worst-case mode: a VERBATIM QUOTATION attributed to Walterová, Běhounková & Efroimsky (2023) that does not exist in the paper** — inside a subsection labeled "(web-verified)". Also: Williams & Boggs's Q=38±4 is at the DRACONIC month 27.212 d, not the anomalistic 27.55455 d (numerically harmless — Q 0.7902→0.7906 — but the whole "forcing-period provenance" subsection was wrong); Nimmo et al. (2012) characterized as arguing the opposite of their actual conclusion (they show melt is NOT required); Bills et al. (2005) Mars Q is 85.58±0.37, with the doc's "~80±1" being Lainey et al. (2007) misattributed — aggravated by a sentence claiming the value was corroborated across every secondary source; a pylov3d result labeled as PyALMA3 and called "a fully independent code path" with no test calling PyALMA3; alpha=0.35 called "squarely inside the commonly-adopted range" that the same document and its own constant define as 0.2-0.3. Two more substantive finds: the two headline Moon assertions **survive a sign-inversion mutation** (Q=-0.79 trivially satisfies `Q<5`), and the gap is closed at FIXED mantle rigidity while an elastic mu_scale≈0.955-0.96 closes the same 4.6% gap with zero anelasticity — the shipped TASK-019 posterior (mu_scale 0.965) already absorbs it, so the headline table overstates. 13-fix round dispatched with an explicit anti-fabrication process rule (no verbatim quote unless retrieved in-session; paraphrase or delete otherwise). **Standing lesson for every future citation task: "web-verified" labels are worthless unless the retrieval happened in the session that wrote them.**
- `[A][2026-08-08]` **Three tracks landed, all UNCOMMITTED pending the user gate.** (1) **TASK-022** Codex ran it: SPDX/Apache-2.0 headers on 90 files, diff verified header-only by A. (2) **TASK-024** Moon field data fetched into `data/moon/` with full provenance — notable: the GRGM900C original's sha256 matches pyshtools' independently-pinned hash, and the shape file's C00 reproduces `moon.MOON["R"]` to 6 s.f. through a code path sharing nothing with the model. Both Moon products had to be locally truncated (no published lmax=120 GRAIL / lmax=719 ASCII shape exists), so SOURCES.md pins the originals' hashes and exact reproduce steps. (3) **TASK-025a** anelasticity build done, Opus adversarial review in flight — the Moon Q result is the science headline and is being attacked specifically for like-for-like comparison (Maxwell on the real 10-layer body vs Andrade on a PyALMA3-simplified body) before anything is claimed. **TASK-023 spec written and unblocked for Codex** (loader hardening + Moon tests; includes making `load_shape` delimiter-agnostic so no future fetch needs to manufacture a reformatted file).
- `[A][2026-08-08]` **TASK-021b VERIFIED** (npz re-analyzed on A, matches A's pins to 4 digits; driver-only diff) — TASK-021 chain fully closed, also marked VERIFIED via B's independent reproduction. **New tracks dispatched:** TASK-024 Moon SH data fetch (A/Sonnet, in progress — unblocks Codex TASK-023 loaders), TASK-025a anelasticity stage a (A/Sonnet build in progress: PyALMA3 viscoelastic validation, Mars+Moon anelastic k2, Moon Q-consistency check; Opus review to follow), TASK-022 Codex license-header spec ready. Proposal repo updated with computed hydration results (SSS_2025_Mars `2e79152`: Figure 3 three-panel, Methods_Models SS2.7/SS4.7, review-verified numbers).
- `[B][2026-08-08]` **TASK-021b DONE — committed `1a0d26a`** (`scripts/mars_hydration_sweep.py`; artifacts under `docs/figures/proposal/mars_hydration_sweep.{npz,png}` — `scripts/output/` is gitignored, so placed with the Mars/Moon MC-posterior artifacts). Independent cross-check of A's TASK-021 at **higher radial resolution** (Nrbase=50 vs A's 30), lmax=4, on independent hardware; full f_h×{low,central,high} sweep, wall 3621 s (15 coupled solves). **Reproduces A's central-ratio numbers to <0.15% on every f_h:** mean/lateral/ratio at f_h=0.1 = 6.3357e-5/9.889e-7/64.1 (A: 6.336e-5/9.89e-7/64), f_h=0.3 = 1.9106e-4/3.136e-6/60.9 (A: 61), f_h=0.5 = 3.2032e-4/5.622e-6/57.0 (A: 57). **mean:lateral 57–64:1 confirmed** — corroborates A's corrected ~60:1 (not 65:1). Detectability reproduced: Δk2 never approaches σ_k2=0.006 (max 5.43% at f_h=0.5); precision to resolve f_h=0.1 = 6.43e-5. **lmax=6 convergence check** (Nrbase=30, central, N 115→219): lateral term shifts only 0.33%/1.09%/2.05% at f_h=0.1/0.3/0.5 — the (4,0)↔(2,0) first-order channel is converged at lmax=4, vindicating A's validated config; residual grows with f_h as expected and is <0.04% of total Δk2. Note: lmax=6 at Nrbase=50 needs >15 GB RAM (near-OOM here), so the convergence check ran at Nrbase=30 (angular question, radial-independent). No `pylov3d` module modified — driver only calls `mars_hydration`. Awaiting VERIFIED (A).
- `[A][2026-08-07]` **Corrections to the entry below + the `370234d` commit message** (caught by an Opus pass while porting TASK-021 results into the proposal docs): (1) D3's fix does NOT raise — it threads `K_variable` through `process_lateral_variations` so the coupling is applied; the regression test pins non-collapse, not an exception (the module's only raise is for an unknown scenario name). (2) The mean:lateral dominance is ~57–64:1 over the swept f_h range (write "~60:1"), not "~65:1". (3) "Lateral ≤1.5% of total" conflates channels: the whole lateral term is <2% (1.72% at f_h=0.5), while the K-convention spread alone is ~0.2% of total. docs/MARS_MODEL.md was already correct on all three; commit message stands as-is (immutable), this entry is the correction of record.
- `[A][2026-08-07]` **TASK-021 DONE — committed `370234d`** (user gate passed). Fix round verified against the reviewer's own measured numbers: D1 dominance restated ~65:1 at validated lmax=4 (500:1 was an lmax=2 truncation artifact); D2 K convention adopted as parity-derived K_nm=3·δK with the ~4× convention band documented (standing caveat: unvalidated in either codebase — raise with LOV3D authors); D3 K-only silent no-op now raises; D4 sign test at lmax=4; D5 normalization pin (kills a −31% mutant); S1 reference-crust sensitivity table (17 GPa ×0.20 … peridotite ×2.40 — dominant systematic, null conclusion robust); S2 reframing ("k2 measures how much hydration, not where; front-diagnostic observable is the off-(2,0) spectrum"). 20 hydration tests green (incl. slow), fast lane 502. Fig7 in proposal set. **TASK-021b cut for B** (independent sweep + lmax-convergence check, claimable now).
- `[A][2026-08-07]` TASK-021 round 1 + Opus dual review (REJECT-narrow, fix round dispatched). Core verified exact; headline conclusion (hydration k2 signal undetectable at current sigma_k2) ROBUST — survives a 12x reference-crust span per the review's own solves. Key corrections in flight: mean/lateral dominance is ~65:1 at validated lmax=4 (the ~500:1 was an lmax=2 truncation artifact — lmax=2 deletes the (4,0) first-order channel); K_amp scaling convention is UNVALIDATED anywhere in either codebase (MATLAB zeroes K_amp on all paths; 3.8x spread across defensible readings; conclusion-insensitive) — flagged as a standing caveat for any future lateral-K work; reference-crust choice is the dominant systematic (InSight in-situ ~17 GPa x0.2 vs peridotite x2.4); framing corrected to "k2 measures how much hydration, not where — the front-diagnostic observable is the off-(2,0) spectrum". Suite 513 full-lane green pre-fix.
- `[B][2026-08-07]` **Claiming TASK-021b** (independent hydration-amplitude sweep, A's TASK-021 headline cross-check). Plan: reproduce A's f_h × serpentinite-bracket sweep via `get_love_hydrated` at higher radial resolution (**Nrbase=50, lmax=4**) on independent hardware, confirm the Δk2(f_h) curve + ~65:1 mean:lateral ratio; then an **lmax=6** convergence check on the (4,0)↔(2,0) channel. Compute-heavy, token-light — reuses `pylov3d/mars_hydration.py` (no pylov3d module modified). Sweep script/artifacts pending user commit approval.
- `[B][2026-08-06]` **TASK-019 DONE — committed `e165ee0`.** Resolved Moon posterior via pocoMC (`scripts/moon_pocomc.py`, Moon analogue of `mars_pocomc.py`): n_active=64, n_effective=128, Nrbase=50, dynamic termination; n_samples=4105, Kish **ESS=4087.7**, wall 123 min. Medians ±1σ: core_rho_scale 0.900 (−0.0146/+0.0278, sits at the 0.88 floor), mu_scale 0.965 (−0.0096/+0.0104), **R_fluid_core 321 km (−13.8/+19.1)**, mantle_rho_scale 1.01 (−0.0012/+0.0011). Median-model observables match all 4 constraints (mass 7.344e22 kg, moi_mean 0.3929, core 321.2 km, k2 0.02419). **R_fluid_core=321 km < stale pre-floor 363.7±33.2 km**, pulled toward the as-built Weber ~327 km: a residual mass/MoI pull under the shipped 0.88 core-density floor, exactly as `moon_mc.py`'s docstring predicts (mass/MoI mechanism, not anelastic). Chain+pairplot committed under `docs/figures/proposal/` (`moon_posterior_chain.npz`, `moon_posterior_pairplot.png`), mirroring the Mars MC precedent. Python suite still green (488 passed) after the pocomc install (mpmath 1.4.1→1.3.0). Awaiting VERIFIED (A).
_(older 08-05 entries — TASK-019/020 claims + DONE notes — pruned; VERIFIED in their ledger rows, full text in git history.)_
_(Older M4 / M5 / TASK-001…013 entries, all four 2026-08-04 TASK-014 part-1/2 claim+DONE notes (MATLAB 1D + coupled Mars cross-check, agreeing to ~1e-12 / 2.95e-13; h2/k2=1.87 confirmed real; eta0-empty-vs-NaN gotcha), the 2026-08-04 proposal-figure-set + TASK-012/013 fix-round notes, the 2026-08-03 M6 direction-change note, and the two 2026-08-05 TASK-016/018 build-round notes pruned — see git history.)_
